---
created: 2026-09-08 08:56:28
branch: main
repo: agentic-toolkit (skills/blueprint, condux bundle)
task: blueprint-diagram-routing-gate
continues-from: none
workstream: blueprint-diagram-quality
---

## Immediate next steps

1. **Docket #76 — calibrate on the real file.** In the repo that produced the "Reporting contract boundary" diagram (a Codex session, 2026-09-08; the file is NOT in this repo and needs Harvey's go-ahead to touch), run `node skills/blueprint/references/diagram-check.mjs <that.html>`. Confirm every visible defect is a finding and nothing false fires. Then compare label widths in the render against the estimate in `skills/blueprint/references/diagram-check.mjs` (`pushTextBox`, `text.length * fontSize * 0.6`); mono at 13px renders slightly wider than 0.6 em (the routed Widget host row touches its border while the checker reads it as inside by one unit). Adjust from evidence, add a fixture, bump condux patch, `pnpm changeset`.
2. **Trigger check in a Codex session.** Ask `/blueprint` for an architecture diagram with 6+ nodes and watch whether the agent runs the checker unprompted before Deliver (SKILL.md Step 2 box, lines 40–47) and fixes findings. If it skips, that is a trigger-contract fix in `skills/blueprint/SKILL.md`, not a checker fix.
3. **Docket #75 when it bites.** A decorative `<line>` (divider, title rule) reports `unlabeled-edge`. Candidate rule in the docket body; do not pre-empt it.
4. **Docket #78 after step 2.** Visual-language redesign (colour by role, node shapes by kind, edge style by protocol, legend). Start as a discovery with three side-by-side options on `specs/blueprint/verification/2026-09-08-routing-gate/routed.html`; any new shape needs a footprint rule in the checker first.

## Blockers

| Blocker | Context | Workaround tried |
|---|---|---|
| none | — | — |

## Current State Summary

condux 2.29.0 shipped and released on every channel: PR #150 squash-merged as 3118965 (tag `condux--v2.29.0` + GitHub release cut by plugin-release.yml), PR #151 "Version Packages" merged as dac28af and `@jabworks/condux` 0.22.0 published to npm by release.yml. The change adds a "Layout and Routing" section to blueprint's diagram kit and `references/diagram-check.mjs`, a dependency-free collision checker blueprint must run before delivering a diagram. Docket #77 closed with the work; #75, #76, #78 filed as follow-ups. Working tree clean on `main`; six stale local and five merged remote branches deleted, only `main` and the bot-owned `origin/changeset-release/main` remain. Nothing in flight.

## Stack snapshot

- **Package / app:** skills/blueprint (source) → dist/plugins/condux, dist/opencode, dist/cursor, packages/condux-opencode (all generated by `scripts/sync.sh`)
- **Layer:** Tooling (agent skill + Node ESM script + node:test)
- **Docker Compose:** N/A
- **Dev server:** not running
- **DB migrations in flight:** none

## Architecture context

### Critical files

| File | Why it matters |
|---|---|
| `skills/blueprint/references/diagram-check.mjs` | The gate. `checkSvg(source)` export + CLI guard; regex-level SVG reader; six finding codes; tolerances 1–2 viewBox units; `distBoxToSegment` is label attribution (box edge, not centre) |
| `skills/blueprint/references/diagram-kit.md` | "Layout and Routing" section (grid ≥72 gutters, 3 ports/side, corridors only, haloed labels, fan-in smell, check step with the codes table) |
| `skills/blueprint/SKILL.md:40-47` | Step 2 PRODUCE: diagrams checked before Step 3, delivered only clean, skipped check said aloud |
| `tests/diagram-check.test.mjs` | 19 tests; `tests/fixtures/diagram-check/collisions.html` reproduces the motivating defects, `routed.html` is the clean twin |
| `specs/blueprint/quirks.md` (Q8), `specs/blueprint/decisions.md` (#8) | Why a checker + rules and not a layout generator (keeps decision #4, hand-authored SVG) |
| `specs/blueprint/verification/2026-09-08-routing-gate/report.md` | Live-verification evidence, promoted from `.condux/` because durable files may not cite working state |
| `scripts/supply-chain-allowlist.json` | Entry for diagram-check.mjs; every shipped script needs one with a reason |

### Key discoveries

- Two false-positive classes surfaced only by review and live verification, not by the unit tests as first written: a `<line …/>` inside the kit's own pasted usage comment became a phantom edge (comments now blanked before the scan), and centre-based label attribution read six of seven real labels as missing (now box-edge distance). Both have regression tests.
- The as-drawn reconstruction of the original also showed row text overflowing its 275-wide boxes — a real defect in the original nobody had noticed. The kit's gutter minimum (72) exists because the original's 57-unit gutters could not hold a 66-unit label.
- `durable-citations.test.mjs` fails on any `.condux/` path in docket bodies or close notes. Promote the artifact into `specs/<name>/` first, then cite that.
- The changesets "Version Packages" PR gets a pull_request CI run that never starts (0 jobs, "workflow file issue") and is marked failed the second the PR merges. Benign: the push CI on the squash commit is what counts. Earlier bot PRs showed `action_required` instead.
- `gh run list --commit <sha>` returned nothing for a completed release.yml run; filter by `--workflow` and read the head sha instead.

## Completed work

### Tasks finished

- [x] Routing rules section in `skills/blueprint/references/diagram-kit.md`; SKILL.md Step 2 check step + "What does NOT happen" row
- [x] `skills/blueprint/references/diagram-check.mjs` (six codes, exit 0/1/2, `--json`)
- [x] 19 tests + two fixtures; suite 505/505; supply-chain + frontmatter gates clean
- [x] specs/blueprint: Q8, decision #8, implementation layout, index changelog, commit stamp at eb8cd83, verification dir
- [x] condux 2.28.0 → 2.29.0, `.changeset/diagram-routing-gate.md`, CHANGELOG entry
- [x] Code review (1 Important + 3 Minor fixed), preflight, finalize, live verification (light + dark via chrome-devtools)
- [x] PR #150 merged, PR #151 merged, npm 0.22.0 live, docket #77 closed, #75/#76/#78 filed
- [x] Branch hygiene: 6 local + 5 remote stale branches deleted

### Files modified

| File | Change | Why |
|---|---|---|
| `skills/blueprint/references/diagram-kit.md` | + Layout and Routing section, path marker note | the missing layout rule |
| `skills/blueprint/references/diagram-check.mjs` | new | the missing feedback loop |
| `skills/blueprint/SKILL.md` | Step 2 check step, not-happen row | gate before Deliver |
| `tests/diagram-check.test.mjs`, `tests/fixtures/diagram-check/*` | new | pin the defects and the idioms |
| `specs/blueprint/*`, `specs/blueprint/verification/2026-09-08-routing-gate/*` | Q8, #8, layout, changelog, evidence | spec ripple + citation rule |
| `scripts/supply-chain-allowlist.json` | entry | shipped script must be declared |
| `dist/**`, `packages/condux-opencode/skills/blueprint/**` | regenerated | sync |
| `docket/DOCKET.md`, `docket/archive/2026.md`, `docket/docket.json` | #75–#78 | backlog |

## Decisions made

| Decision | Options considered | Chosen | Rationale |
|---|---|---|---|
| Shape of the fix | rules only / rules + checker / JSON→SVG layout generator | rules + checker | rules alone are a hope (agent can't see its render); a generator is LARGE and reverses decision #4; same shape as Q7's token allowlist (Harvey's pick) |
| Execution | implement myself / coder agents | two parallel coder subagents (sonnet) | Harvey's pick at CP-1; disjoint files, interfaces fixed in briefs |
| Label attribution metric | text-box centre / text-box edge | box edge (`distBoxToSegment`) | real diagrams put long start-anchored labels beside vertical lines |
| Owned-text crossings | report title + box / box only | box only when the edge is not attached | one defect printed once |
| Evidence promotion | cite `.condux/` / promote to `specs/` | promote text artifacts, keep PNGs + generator in working state | durable-citations rule; HTML re-renders the evidence |
| Second commit for spec stamp | amend / follow-up commit | follow-up (d58edc3) | matches 9a0f115's precedent; sha can't be known before commit |

## Important context

- Edit `skills/<name>/` only; `dist/`, `dist/opencode`, `dist/cursor`, `packages/condux-opencode/skills` are generated by `bash scripts/sync.sh <name>` (pre-commit hook re-syncs). Any condux skill edit needs `pnpm changeset` or `npm-channel.test.mjs` fails and npm silently lags.
- Version lives in `dist/plugins/condux/.claude-plugin/plugin.json` and `.codex-plugin/plugin.json` (hand-edited, both); condux has no root plugin.json because it ships hooks — never add one.
- `node scripts/release-plugins.mjs --write-changelog` writes a placeholder subject until the commit exists; re-run after committing (it also normalises older entries, e.g. appends "(#147)").
- Checker limitations, documented in Q8: text width ≈ 0.6 em/char; curves reduced to endpoints; a free label fully inside a box's empty area reads as owned and passes; an edge running through a label becomes that label's "own" edge (the spot still fires `edge-through-box` + `label-over-box`); nested inset rects (state-machine terminal states) make the outer rect a "boundary".
- Harvey's process preferences observed this session: route through `/condux:workflow` and confirm the tier; present the full CP menus; file (and close) a docket item for completed work before commit + PR, and file follow-ups he names as new items (memory: feedback_docket_before_commit).
- Repo boundary: the original diagram's HTML lives in another repo — ask before reading it.

## Deferred / out of scope

- Running the checker on the original diagram HTML (#76) — other repo, needs Harvey.
- Decorative-line rule (#75) — wait for a real case.
- Visual-language redesign (#78) — after the gate is seen holding in a real Codex session.
- `processTextElement` mixed positioned/unpositioned `<tspan>` children: unpositioned ones are dropped, not folded into the parent box. Not observed in any kit diagram.
- `.condux/verification/2026-09-08-diagram-routing-gate/` (generator + PNGs) and `.condux/plans` / `.condux/progress` for this task remain as gitignored working state; safe to delete.
