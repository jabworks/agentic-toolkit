---
"@jabworks/condux": minor
---

plan-review: directory mode's sidebar is now a doc-site tree — collapsible folders (collapsed except the active doc's ancestors, note counts rolled up), a filter matching doc names and headings, breadcrumb, and prev/next navigation. Single-file plan review is unchanged (docket #22).
