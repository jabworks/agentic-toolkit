---
"@jabworks/condux": patch
---

plan-review: directory mode docs said "every top-level `*.md`"; the server walks the whole tree (grouped by folder, `index.md` first, dotdirs skipped). Prose now matches the code (docket #24).
