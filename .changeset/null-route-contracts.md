---
"@jabworks/condux": patch
---

workflow + draft-plan: widen the trigger contracts against the null-route error mode (docket #57) — workflow now claims any dev task at any size (tests, CI/config, docs edits, one-liners), draft-plan names its `.condux/plans/` artifact and routes design-less plan requests to its own soft gate
