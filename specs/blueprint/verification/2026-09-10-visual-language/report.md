# Live verification: diagram visual language (blueprint, condux 2.31.0)

The 2026-09-08 routing gate proved the kit's Layout and Routing rules on a real diagram. This run does the same for the Visual Language — colour is role, mark is kind, dash is protocol — on the *same* diagram: identical nodes, edges, labels and coordinates, restyled. Geometry never moved, so any difference between the two files is the language and nothing else. The specimen and its findings are beside this file; the discovery generator (working state, not committed) emitted them, and the screenshots stayed in working state.

| | |
|---|---|
| Date | 2026-09-10 |
| Target | `node skills/blueprint/references/diagram-check.mjs specs/blueprint/verification/2026-09-10-visual-language/reporting-visual-language.html`, rendered headless from `file://` at 2300×1200 (the brief's size, which puts the legend below the fold) and again at 2300×1700, where the legend is visible |
| Diff | branch `feat/diagram-visual-language`, PR #155 |
| Themes | light ✓ dark ✓ |

| Claim | Evidence | Verdict |
|---|---|---|
| The restyled specimen passes the checker | `findings.txt` beside this file | ✓ `clean`, exit 0. The 2026-09-08 `routed.html` was clean before the restyle and is clean after it: the language costs the drawing nothing |
| It renders in light and in dark with every edge in a gutter and every label haloed | `reporting-visual-language.html` opened in Chrome, once plain and once with `data-theme="dark"` on `<html>` (screenshots observed in-session, not committed) | ✓ both. No segment crosses a node body; all thirteen labels sit on a `var(--card)` halo, which is the frame's own ground in this language, so labels read identically in the two themes. The three edge-to-edge crossings near Widget host that the routing gate permitted are still there and still legible |
| Every node is tinted by its slot, and the ten nodes span four roles | the specimen's node rects; the legend's `role` row | ✓ every node body carries `fill="var(--cat-N)" fill-opacity="0.10" stroke="var(--cat-N)"`. Four slots used: frontend runtime `--cat-4` (Resolved composition, Frontend catalogs, Filter + query planner, Result validator, Widget host), API / service `--cat-2` (Composition resolver, Filter options API, Reporting query API), persistence `--cat-3` (Layout store), external system `--cat-7` (Upstream systems). Four is inside the five-role cap. **Ten nodes, not twelve** — see "Also seen" |
| The accent node alone carries `--primary` | the `<svg>` block | ✓ exactly one `var(--primary)` and one `var(--primary-text)` in the whole drawing, both on Composition resolver: a 2.5px `--primary` stroke over its kept `--cat-2` tint, and a `--primary-text` title. `--primary-muted`, which the pre-restyle file used on two nodes, occurs zero times. Upstream systems, accented in the original, is now plain `--cat-7`: it is not what this diagram is about |
| Kind marks are the kit's, verbatim | the specimen's `<defs>`; `tests/diagram-check.test.mjs` | ✓ the kit's `### Marks` fence is read out of `diagram-kit.md` at emission and injected whole, so the six `<g id="mark-…">` groups are byte-identical to the kit's *by construction*. The generator asserts the count is six and that no `fill="transparent"` survives; an inclusion check of the whole fence against the emitted file confirmed the identity in-session; the new fixture test asserts the six ids are present in `<defs>`. Four are `<use>`d: `mark-store` on Frontend catalogs and Layout store, `mark-ui` on Widget host, `mark-external` on Upstream systems. `mark-actor`, `mark-queue` and `mark-batch` ship unused inside `<defs>`, which the checker skips |
| Protocol is carried by the dash, and only by the dash | the specimen's edge paths | ✓ thirteen edges, `stroke="var(--subtle)"` on every one and no colour anywhere on an edge: five sync calls (1.5 solid — every `HTTP ·` label), seven in-process hand-offs (1 solid), one adapter transfer (1.5, `2 3`) — the `source adapter protocol` edge into Upstream systems |
| The legend rows match exactly what is drawn | the legend block under the `<svg>`; both renders | ✓ four rows, the kit's markup and its CSS verbatim. `role` lists the four slots used and no others; `kind` lists service / store / UI surface / external system, the four kinds present, each swatch `<use>`-ing the same mark id the drawing does; `edge` lists the three protocols present, with the `2 3` adapter shown as CSS `dotted` per the kit's approximation note; `accent` is one row. The "queue / event (none here)" row the option mockups carried is gone — a row you cannot point at is a defect |
| No real system name survives | the specimen and its fixture copy; the fixture-hygiene test | ✓ Upstream systems reads `Billing · Inventory · CRM` / `Payroll · future reporting sources`. The generator throws if any of the four original names survives sanitizing, and the hygiene test in `tests/diagram-check.test.mjs` now greps every fixture for all four |

Also seen:

- **The task brief said twelve nodes; there are ten.** The routed specimen has exactly ten `rx="6"` node rects, and the generator's own count agrees. Four roles across ten nodes is the true shape — nothing was dropped in the restyle.
- **A label halo clipped the frontend title strip by 2.25px in the first emission.** The `HTTP · effective composition JSON` corridor ran at y 300 with its halo from 285.75, while the strip ends at 288 — the inherited 2026-09-08 routing, drawn before strips existed. Fixed in this redraw: the corridor moved to y 303 (halo 288.75–303.75, label baseline 299), so the specimen now honours the kit's Boundaries rule. The checker has no notion of a strip band, so the overlap was invisible to it — docket #82 keeps the candidate rule.
- **`--cat-7` and `--primary` are close in hue.** Both are warm tans. In dark especially, the external node's dashed `--cat-7` border and the accent's solid `--primary` border sit in the same colour family; they stay distinguishable by weight (2.5px vs 1) and by dash, not by hue alone. A diagram that accented an external node would read ambiguously.
- **Widget host's second row is flush, not overflowing.** `joins placement + definition + data` ends at x 622.91 against a box edge at 623 — 0.09 units of slack. The checker reads it as inside and it renders touching the border. The 2.30.0 font-size work made this measurement exact rather than approximate, so this is now a real "as tight as it can be", not the calibration gap the routing-gate report flagged.
- Boundary title strips moved to the kit's geometry: 48 tall, title 24px in from the boundary's left edge with its baseline at strip top + 30, in the boundary's role colour rather than `--muted-foreground`. That is the strip's own style; no node, edge or label coordinate changed. Members still start 45px below the strip, exactly the kit's floor.
- The `<svg>` now carries `font-size="13"`, per the kit's canvas template. It is inert for this specimen — every `<text>` sizes itself (16 titles, 14 boundary titles, 13 rows, 11 labels), so nothing inherits it and no checker measurement changes. It matters for the next diagram drawn from the template, not this one.
- The canvas keeps the routed diagram's empty top band (viewBox y 0–240 is unused). Inherited geometry, not introduced here.
- No console errors; no network requests (`file://`).

0 claims failed. All green.
