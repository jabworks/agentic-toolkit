# Spec Index

> Generated by spec-browser on 2026-08-26 — do not edit by hand.
> Regenerate: `node <spec-browser>/references/build-index.js specs`

11 specs.

| Spec | Purpose | Path |
|---|---|---|
| [Agent Plugins conformance](agent-plugins-conformance/index.md) | Every dist plugin is a spec-conformant Agent Plugin ([agent-plugins.org](https://agent-plugins.org) 1.0.0): root `plugin.json` (generated), skills as immediate children of `skills/`, docket MCP via… | `agent-plugins-conformance` |
| [blueprint — Tech Spec](blueprint/index.md) | condux skill producing dependency-free visual clarity artifacts at design time — HTML wireframes and renders for UI in the surface-kit token language (two modes, one skeleton), inline-SVG system… | `blueprint` |
| [CompositionManifest — Tech Spec](composition-manifest/index.md) | Plugin composition declared as data: `composition.json` is the single source for bundle membership, plugin-level dirs, and marketplace descriptions, feeding sync, `marketplace.json`, and the doc… | `composition-manifest` |
| [Concord — Tech Spec](concord/index.md) | Continuous memory for Codex — captures sessions, compresses them into aging tiers, and injects the relevant slice back at every session start. Clean-room MIT; `remember` keeps Claude Code. | `concord` |
| [Cursor distribution channel](cursor-channel/index.md) | The toolkit's fourth distribution channel: Cursor consumes skills as native SKILL.md (since Cursor 2.4) from a merged-trigger tree at `dist/cursor/skills/`, generated by the shared variant builder. | `cursor-channel` |
| [dir-mode-navigation — Tech Spec](dir-mode-navigation/index.md) | Doc-site navigation for plan-review's directory mode: recurse the tree and group the Contents nav by folder, so a whole `specs/` tree reads as one site rather than a flat list of top-level files. | `dir-mode-navigation` |
| [Docket — Tech Spec](docket/index.md) | File-based project backlog as a plugin bundle: two skills (`docket:record`, `docket:groom`), a dependency-free CLI (`docket.mjs`) as the single source of truth for mechanical ops, a thin stdio MCP… | `docket` |
| [Plugin doctor — Tech Spec](plugin-doctor/index.md) | A per-plugin health check for the three plugins with machinery beyond skill files — condux, concord, docket. | `plugin-doctor` |
| [PreflightDriftCheck — Tech Spec](preflight-drift-check/index.md) | Preflight's drift check: compare the implementation against the task's spec before finalize and surface both directions of drift as soft-gate findings, so a spec and its code cannot silently diverge. | `preflight-drift-check` |
| [surface-kit — Tech Spec](surface-kit/index.md) | Shared design system for the toolkit's four self-contained HTML surfaces: extends the colour core into type, space, radius, motion and elevation, and generalizes `check-tokens.mjs` from one inlined… | `surface-kit` |
| [uninstall-convention — Tech Spec](uninstall-convention/index.md) | The removal half of the toolkit's ease-of-install convention. An agent-followable `UNINSTALL.md` at each plugin root (condux, docket, concord) over `--uninstall` machinery that lives in the… | `uninstall-convention` |
