# DOCKET ARCHIVE 2026

Closed items with their verification records. The id space is shared with the open file — ids are never reused.

## 2 + 17. UNINSTALL.md — the removal half of the ease-of-install convention, and condux's --uninstall gap (2026-08-05 / 2026-08-11) — ✅ DONE 2026-08-12

Closed together, as the entries insisted. #2 was the documented procedure and #17 the machinery underneath it, and the question they shared — what a plugin may do about host state it sets but does not own — is policy that no amount of machinery answers.

**Ruling: an uninstaller reverses only what it exclusively owns.** `[features] hooks = true` has three writers (condux's front door, concord's hook installer, plan-review's hook installer) and no owner, so nobody clears it. This promotes behaviour that already existed into a stated rule: condux's source already carried the comment, but nothing a *user* read said so, which makes a correct outcome look like an incomplete removal. Every front door now names the other riders, and the Codex row reports `warn` rather than `done` so the residue is visible instead of silent. Reference-counting was rejected — it needs each plugin to carry a map of its siblings' registration shapes, the cross-plugin knowledge the no-plugin-deps rule bans, and it fails in the dangerous direction when a probe is wrong. Prompting the user was rejected as moving a decision onto someone with less information than we have.

**Machinery lives in the writer, not the front door.** The deciding evidence was concord's existing `--uninstall`: not a separate routine but a `MODE` flag inside the same script, reusing its own detect and report. Reversal is cheap there because the writer already knows the shape of what it wrote. So plan-review's hook installer, docket's `install.sh`, and the Codex agents installer each gained the same mode, and condux's front door delegates to them — symmetric with install, which already delegated. Reversing from the front door instead would have meant re-deriving the agent TOML names and the Stop hook's entry shape, and scattered install knowledge is exactly what made condux's install story wrong until #9.

Two corrections found by integration review rather than by test. docket's uninstall wrote a temp file and `mv`'d it over `config.toml`, silently widening 0600 to the caller's umask on a file that holds Codex credentials; it now rewrites through the existing inode so the user's mode survives, asserted. And its verify beat still reported "server answers initialize", which is true after an uninstall and proves nothing — it now confirms no registration remains and fails if one does. A third came from the agent doing better than its brief: agent names are derived from `.md` frontmatter rather than filenames, so a rename cannot desync install from uninstall.

`.bak` is never restored, verified rather than assumed: `install.sh:47` rewrites the backup at the start of every run, so after a second install it holds an already-registered config and restoring it would reinstate the very registration being removed. Removal is surgical.

Verification: `node --test` 240 → 241, supply chain clean over 180 files, sync clean. Round-trips per installer in sandboxes, 18 assertions total: shared flag survives every path, sibling registrations survive (concord's `SessionStart`, an unrelated `Stop` entry, `[mcp_servers.other]`, opencode's `theme`, a hand-placed agent TOML), `config.toml` stays 0600, `--dry-run` writes nothing (whole-sandbox hash before and after), second runs exit 0.

The guard work found a hole in itself, which is the part worth remembering. Widening #19's front-door test to `UNINSTALL.md` looked sufficient and was not: the rule only fires when a document of that kind exists *somewhere* in the tree, and no plugin has a buried `UNINSTALL.md` — concord's removal steps live inside its deep `INSTALL.md`, docket's did not exist at all. So deleting all three front doors passed. Placement was guarded, existence was not. A separate test now keys the obligation off the install front door: a plugin that tells a user how to register something owes them the reverse. Found by mutation, which is why mutation-testing every new guard is the standing rule and not a formality.

One test edited with sign-off rather than silently: `tests/script-safety.test.mjs:220` asserted the old copy `/shared with other plugins/`. The three behavioural assertions in that test were already passing; only the phrasing had changed. It now asserts the report names the actual riders, which is strictly stronger and matches the ruling.

Versions split three ways rather than moving together, through toolkit-change-control: condux 2.13.0 → 2.14.0 and docket 0.3.2 → 0.4.0 (skill edit, new capability — uninstall genuinely works where it did not), concord 0.4.1 → 0.4.2 (its installer already had `--uninstall`; this ships the document only, so doc-inside-a-plugin → patch, the same call #19 took).

Design and spec: the design was working state and is gone (docket #34); `specs/uninstall-convention/` and PR #51 are the surviving record.

## 19. INSTALL.md is buried for docket and concord, at the plugin root only for condux (2026-08-11) — ✅ DONE 2026-08-12

**Wrap**, following #9's precedent for condux. The entry named a check to run before choosing, and running it decided the item: *does anything resolve paths relative to the INSTALL.md's own location?* Both deep documents do, in their first step — `skills/remember/references/INSTALL.md:18` defines `SKILL` as "this file's parent's parent", and `skills/record/server/INSTALL.md:19` defines `SERVER` as an absolute path to `mcp-server.mjs` "in this directory". So **Move** is out: relocating either silently breaks its own step 0, on top of #9's standing argument that `plugins/` never reaches the `npx skills add` tree. Contrast condux's plugin-level document, which is stable precisely because it anchors on `$PLUGIN_ROOT` and sits at that root.

**Generate** is weaker than it looks, and this is worth recording because #12 will make the same argument look attractive again. A generated *copy* inherits the broken document-relative base — that is the same defect as Move, arrived at by a build step. A generated *pointer* cannot derive the per-plugin host table, which is the only content a user actually needs at the root: concord registers on Codex alone and reports `skipped` twice; docket skips Claude Code because the plugin's own `.mcp.json` covers it. Generation would produce a stub that says less than the wrap it replaced. Generate-over-test is right when the target is derivable; here it is not.

Two front doors written, each carrying the host table, the one-command path, the doctor invocation, and a source-layout note saying which copy to edit. concord's also records *why* it is a pointer rather than a copy, since that is the trap the next person will walk into. No new sync machinery: `sync_plugin_files` already copies every regular file in `plugins/<name>/` and `plugin-files.test.mjs` guards each byte-for-byte.

Two tests, both generalised past the two plugins that prompted them. `a plugin whose install procedure is buried also has one at its root` walks the shipped tree for any `INSTALL.md` and fails when none sits at the plugin root — so a future plugin that buries its procedure is caught without anyone remembering this item. `every path a plugin-level INSTALL.md cites resolves in its dist tree` is the honesty half of the wrap: the front door's whole job is naming where the real procedure lives, so a stale path in it is worse than no front door. It resolves against the dist plugin root (what a user lands on) and the repo root (so "edit the source, never the mirror" notes are not read as dangling), skipping placeholders and host-machine paths.

Verification: `node --test` 238 → 240, both new tests triggered to fail by mutation — deleting `plugins/docket/INSTALL.md` fails the first, renaming `server/install.sh` to `server/setup.sh` inside the front door fails the second. sync clean with no drift. Classified through toolkit-change-control as doc-only-shipping-inside-a-plugin → **patch**, not the minor #9 took: docket 0.3.1 → 0.3.2, concord 0.4.0 → 0.4.1, both manifests each, CHANGELOG regenerated.

One coupling to know about: `tests/skill-supply-chain.test.mjs` (from #16) asserts that `plugins/concord/README.md` still contains the literal `` `references/INSTALL.md` `` citation, as the regression fixture for cross-tree citation resolution. The front-door pointer was added *alongside* that citation rather than replacing it. Removing it later fails that test with "the citation this guards is gone" — which is the intended signal, not a bug.

Sets the shape for #2: `UNINSTALL.md` should be a plugin-level wrap over per-plugin removal procedures, for the same reason. #17 (condux's `--uninstall` gap) is the machinery question underneath it and is still open; both front doors state the removal path in prose and name #2 as the standing item.

## 16. Supply-chain lint does not cover plugin-level files (2026-08-11) — ✅ DONE 2026-08-12

`scripts/check-supply-chain.mjs` walked `skills/` only, so the repo's first plugin-level executable (`plugins/condux/install.mjs`, shipped by #9) and the `INSTALL.md` instructing an agent to run it were scanned by nothing — all four of the rules that exist for exactly that shape were reachable in a tree the checker never opened.

#18 predicted this would be #11-shaped work because "the allow-list keys and its inventory both assume `skills/`". It was not. Both are keyed to repo-relative source paths already, so they took a `plugins/` prefix unchanged and the script inventory needed no edit at all. The real coupling was one constant doing two jobs: `SCAN_ROOT` named both the tree to walk and the tree to resolve `references/…` citations against. Those are different questions — skills are the only tree that ships `references/` helpers, and plugin-level prose cites them by the same relative shape (concord's plugin README says `references/INSTALL.md`, meaning `skills/remember/references/INSTALL.md`). Split into `SCAN_ROOTS` and `SIBLING_ROOT`; a probe that widened the scan without splitting them reported that live link as dangling, which is how the trap was found before it shipped rather than after.

Allow-list written against real findings, not guesses: 11 findings across 4 codes over the 14 new files, closed by 2 entries. `plugins/condux/install.mjs` (SCRIPT-FILE) also clears the 8 INVOKES-SCRIPT findings on the `node … install.mjs` lines in `INSTALL.md` and `README.md`, since invocations match by basename — the coupling is recorded in the entry's reason so a later reader does not delete one and silently lose the other. `www.npmjs.com` (EXTERNAL-DOMAIN) is the `@jabworks/condux` package page cited in prose; nothing fetches it.

The entry's open question is answered: docket's installer machinery is seen **once**, at `skills/record/server/`. `plugins/docket/` and `plugins/concord/` are README-only, and the plugin-root copy lives in `dist/`, which this checker deliberately does not walk. 175 files = 161 skills + 14 plugins, no overlap. That `dist/` exclusion is unchanged and still correct — byte-parity is guarded by `dist-mirror.test.mjs` and keying the allow-list to source paths keeps one reason per fact.

Code review raised one Important finding, fixed in branch: `collectFiles`' second parameter changed from a single root string to an array, and it is exported. A stale string caller would iterate the string character by character, match no directory, and hand back an empty list — the checker would then report clean over zero files, which is the one failure a security gate cannot afford and which this file's own test comment already names ("a checker that silently scans nothing passes forever"). A string is now normalised to a single-element array, with a test that fails without it. Left as noted, not fixed: declaring `plugins/condux/install.mjs` whitelists the *basename* `install.mjs` for INVOKES-SCRIPT everywhere, since invocations match by basename. The blind spot is narrow — an actual new `install.mjs` file still trips SCRIPT-FILE, which is keyed to the full path, so only prose naming an `install.mjs` this repo does not ship gets through — and `doctor.mjs` and `install.sh` set the precedent already.

Verification: `node --test` 235 → 238, checker clean over 175 files (was 159 at #13, plus 2 from #18). All three new tests triggered to fail by mutation, not assertion: narrowing `SCAN_ROOTS` back to `['skills']` fails 2 (the plugins-coverage anchor, and the live assertion — because the 2 allow-list entries go unused, which the reasonless-AND-unused rule from #13 catches on its own), and resolving citations against the citing file's own tree fails 3 (both citation regressions plus the live assertion). The coverage test anchors on the executable *and* the prose that runs it, because the existing skills-tree assertion passes at full strength on a scan that opens neither. No version bump — `scripts/` and `tests/` only, no skill or plugin file touched, same call as #15.

Still open, adjacent: #19 (INSTALL.md is buried for docket and concord) and #17 (condux `--uninstall` cannot reverse what its sub-installers wrote). Neither is affected by this — the checker now reads whatever those land, wherever they land.

## 18. Warn about conflicting skill libraries at install and doctor time (2026-08-11) — ✅ DONE 2026-08-11

condux is a rework of obra/superpowers and said so in one line of README acknowledgment, which is credit, not a warning. Installing both is not additive: verified against superpowers 6.2.0, the two register a SessionStart hook on the same matcher (startup|clear|compact), each injecting a router that claims every dev task, so the agent follows whichever it read last — and 11 of superpowers 14 skills answer the same question as 8 of condux 14. Nothing errors, which is why it needs saying out loud.

Decided before building, all three the conservative branch: report the removal command rather than run it (reversing another vendor registration is outside what this installer registered, and a cross-plugin removal path is #2); condux only (the sole plugin with a plugin-level installer and the sole derived-from conflict); and a hardcoded registry matched by name, not a semantic comparison of descriptions — that is #10, open and undecided, and a doctor advertising itself offline and read-only is not where it gets prototyped.

Verification: shipped in condux 2.13.0. Registry is data (skills/condux-doctor/conflicts.json) read by both callers through a shared conflicts.mjs, so the table naming another project skills exists once — a test fails if either script names a conflicting library in code. Report grammar gained a fifth status, warn: a competing library is not broken (condux installed correctly, and the installer verify beat keys its exit code on broken) and done would be a lie. Two detection surfaces, because a machine can carry the overlap either way: host plugin registrations (installed_plugins.json keys, config.toml [plugins."name@marketplace"] headers) and the four loose skills directories, with a floor of 2 matching names so a lone brainstorming/ accuses nobody. Registrations are read rather than plugins/cache/, which outlives the install that created it.

node --test 223 → 235 (12 new). Each triggered to fail before passing, by mutation rather than assertion: warn→broken failed 3, floor→1 failed the false-positive guard, dropping the doctor row failed the report check, hardcoding the name failed the single-source check, removing the resolve check failed the dangling-symlink case, and adding one of our own skill names to the registry failed the self-collision guard. That last guard came out of code review: the detector matches directory names, so a registry name equal to a skill we ship would accuse every clean condux install of conflicting with itself. There is no overlap today because 2.0.0 renamed condux off superpowers vocabulary on purpose; the test is what keeps that true.

That last test exists because running it live found a false positive the design had not anticipated. The first detector read ~/.claude/skills with readdirSync and counted names, and on this machine reported 14 superpowers skills present. All 14 were symlinks into a ~/.agents/skills tree that had since been pruned — dead links a host cannot load. Detection now follows the link (existsSync is false for a dangling one) and reports the resolved parent directory rather than the one holding the link, since that is where removing anything would have an effect. The corrected run reports the plugin surface alone, which is the truth here, and still exits 0. conflicts.mjs is allow-listed in the supply-chain scanner with its narrowness as the reason: filesystem reads and name matching, no network, no child process, asserted by test.

Not done here: Plannotator (plan-review lineage) is documented in the README but not in the registry — no detection surface for it was verified, and a wrong name in a shipped warning is worse than no warning. The removal half across all three plugins remains #2.

Still open, unchanged by this: scripts/check-supply-chain.mjs walks skills/ only (its own comment at line 185 says so), so plugins/condux/install.mjs is statically unscanned — the gap #9 recorded when it shipped the first plugin-level executable. This item widened that file rather than that gap: the dynamic import it gained resolves through the same findSibling probe as the doctor it already invoked, and tests/script-safety.test.mjs:119 covers the installer behaviourally. Teaching the scanner a second tree is #11-shaped work — the allow-list keys and its inventory both assume skills/.

## 3. Trigger eval cases for record/groom (2026-08-05) — ✅ DONE 2026-08-05

Accepted drift at preflight 2026-08-05 (specs/docket/quirks.md): near-miss trigger eval prompts for record vs groom on both sides of the item-level vs whole-backlog boundary, wired into scripts/eval-triggers.mjs.

Verification: shipped 1a2cfef on feat/docket-trigger-evals; 150/150 tests green

## 6. Graduate the no-plugin-deps rule to a degrade-gracefully ladder (2026-08-05) — ✅ DONE 2026-08-06

Amend toolkit-skill-standards (and the no-plugin-deps memory rule): skills prefer the highest detected rung — (1) skill files, always present on every channel; (2) bundled CLI travelling with the skill (record's server/ precedent); (3) registered MCP, host-dependent — but must function on rung 1 alone, because npx skills add ships bare skills with no manifest. Cross-plugin dependencies stay a hard ban. INSTALL.md makes rung 3 reliable to reach; plugin doctor (#1) reports which rung each plugin actually runs on per host. Decided 2026-08-05 in session.

Verification: Dependency ladder section + procedure step 9 in toolkit-skill-standards (toolkit-ops 1.5.2); memory rule updated same day; 150/150 tests green

## 1. Plugin doctor — verify condux/concord/docket work on the installed harness (2026-08-05) — ✅ DONE 2026-08-06

Per-harness health check for the shipped plugins (condux, concord, docket): does the condux SessionStart hook actually fire on this host, are concord rollout-sync hooks wired, does the docket MCP server answer an initialize round-trip, do the manifests the installed host reads parse, do installed versions match the marketplace. Shape: detect host, probe each registration, report done/broken/absent per plugin — the standing version of the INSTALL.md verify step. Precedents: ctx doctor, remember:doctor. Origin: idea recalled 2026-08-05 after shipping docket.

Verification: docket-doctor, condux-doctor, concord-doctor shipped; health-check convention written into toolkit-skill-standards; 25 doctor tests green, verified against real plugin installs on this host

## 5. INSTALL.md adoption for condux and concord (2026-08-05) — ✅ DONE 2026-08-06

Adopt the INSTALL.md ease-of-install convention (detect, register, verify, report) for the other complex plugins: condux (OpenCode npm story) and concord (Codex hooks). Docket server/INSTALL.md is the reference implementation.

#### Status 2026-08-06 — second payoff: it unblocks `--fix` in the doctors

The plugin doctors shipped (#1) with repair deliberately split: every broken probe prints its fix, but only `docket-doctor` can *perform* it, because `--fix` delegates to `server/install.sh` rather than reimplementing registration. `condux-doctor` and `concord-doctor` print their fix and stop — they have no installer to delegate to. Writing those installers is exactly this item, so it now buys the ease-of-install convention and the repair half of the health-check convention in one change. `concord-doctor` already names `references/install-codex-hook.sh` as the fix for an unwired host; that script is the seed of concord's INSTALL.md.

Verification: split into #8 (concord) and #9 (condux) after a survey found the two halves are very different sizes; no work shipped under this id

## 8. INSTALL.md + verify for concord (split from #5, 2026-08-06) (2026-08-06) — ✅ DONE 2026-08-06

concord is the small half of #5 and is mostly done already.

Has: `references/install-codex-hook.sh` — merges the three hooks into
`<CODEX_HOME>/hooks.json` with absolute paths, enables `features.hooks`,
supports `--uninstall`, refuses to overwrite malformed JSON, prints `ok`/`set`
lines.

Missing three things:

1. A verify step. It registers and stops — it never confirms the thing it
   registered answers, which is the exact failure the ease-of-install
   convention exists to catch and the reason docket's installer round-trips
   its server.
2. `INSTALL.md` — the agent-followable twin, for restricted shells or when the
   user wants to see each step.
3. The convention's `host status detail` report format instead of `ok`/`set`.

Verify is nearly free: `concord-doctor --host codex` already implements every
probe and lives in the same plugin, so the installer may call it — no
cross-plugin dependency. Installer registers, doctor confirms.

Then wire `concord-doctor --fix` to the installer, which closes the repair half
of the health-check convention for this plugin.

Self-contained, no open design questions — the second worked example of the
convention after docket.

Verification: concord 0.4.0. Installer rewritten to the ease-of-install convention (detect → register → verify → report, `host status detail` at widths 10/8); `references/INSTALL.md` added as the by-hand twin; `concord-doctor --fix` delegates to the installer. Verify prefers the sibling doctor (`--host codex --quiet`, same plugin, no cross-plugin dep) and degrades to reading `hooks.json` back when `npx skills add` ships the skill without it — saying which path it took. Verified: installer run end-to-end in a temp CODEX_HOME (fresh, re-run, --uninstall, no-doctor fallback), `--fix` turns the absent Codex row green; 5 new tests in plugin-doctor.test.mjs, suite 197 → 202 green.

## 13. Generalize supply-chain lint across all skills, not one template (2026-08-09) — ✅ DONE 2026-08-09

We already do one of these checks — for exactly one file. `skill-invariants.test.mjs:105`
("plan-review HTML template makes no external network references") greps that
single template for `//` URLs. That is the `EXTERNAL-DOMAIN` rule, hand-scoped
to one artifact because that artifact was a known risk. Nothing generalizes it,
and no other rule in the family exists at all.

Adjacent but different: `script-safety.test.mjs` tests the *runtime* behaviour
of three specific installer scripts (path escape, idempotency, refusing to
overwrite malformed config) and `browser-security.test.mjs` tests plan-review's
HTML escaping. Both check that our known scripts behave; neither reads a
SKILL.md to see what it instructs an agent to fetch or execute. That is the
gap — and we publish to a public marketplace across three channels.

Upstream runs `npx @microsoft/vally-cli lint ./skills` nightly
(`.github/workflows/skill-quality-report.yml`) and gates PRs with
`skill-check.yml`. Vally's lint (MIT, read from the 0.13.0 tarball —
`dist/skill/reference-scanner.js` + `external-dep-checker.js`) is purely
supply-chain, driven by two allow-list files (`knownDomainsFile`,
`allowedExternalDepsFile`):

| code | flags |
|---|---|
| `EXTERNAL-DOMAIN` | URL to a domain not on the known list |
| `HTTP-NOT-HTTPS` | plaintext HTTP reference |
| `PIPE-TO-SHELL` | `curl ... \| sh` shaped instructions |
| `INVOKES-SCRIPT` | skill body tells the agent to run a script |
| `SCRIPT-FILE` | bundled executable script |
| `SCRIPT-NO-SRI` | remote script without subresource integrity |
| `NON-BUILTIN-TOOL-REF` | references a tool outside the host's builtins |
| `FILE-READ-ERROR` | referenced file unreadable |

Several of ours trip these on purpose — docket ships `install.sh` and
`server/mcp-server.mjs` behind `.mcp.json`, condux ships `session-start.mjs` as
a SessionStart hook. That is the argument *for* an allow-list rather than
against the check: the value is that a *new* unreviewed one shows up in CI.

Adoption shape, per the no-plugin-dependencies ladder: do **not** take the CLI
dependency. Port the eight rule codes into a dependency-free
`tests/skill-supply-chain.test.mjs` with a checked-in allow-list, written the
way `frontmatter-canonical.test.mjs` is — no devDependency, same module usable
from the pre-commit hook. (`@microsoft/vally-cli` would be a repo dev
dependency, not a shipped-plugin one, so it is defensible; it is just more than
this needs.)

Cheapest of the awesome-copilot findings to land. The plan-review no-egress test
is the proof it works and the thing to subsume: once the general check exists
with plan-review's template on the allow-list, that bespoke test can retire.

Found 2026-08-09 surveying awesome-copilot's maintenance machinery.

Verification: shipped as PR #41 (squash 0296f73). scripts/check-supply-chain.mjs ports 7 of 8 vally rule codes dependency-free; NON-BUILTIN-TOOL-REF deliberately omitted (no canonical host-builtin list) and SCRIPT-NO-SRI renamed UNPINNED-REMOTE-REF, both recorded in the file header. Allow-list fails on reasonless AND unused entries. Verified: node --test 213/213, checker clean over 159 files, manifest gate green. First real catch: session-report and session-handoff templates were fetching Google Fonts at render time — page-caused external requests 23 -> 0 per template, measured by chrome netlog against git show HEAD: copies, screenshots identical. Released session-report--v1.6.4 and session-handoff--v1.6.7. The one-file plan-review no-egress test was retired into the general rule.

## 15. Three cheap skill-validation checks we do not have (2026-08-09) — ✅ DONE 2026-08-10

Small additions to `tests/skill-invariants.test.mjs`, each a few lines, all
lifted from `eng/validate-skills.mjs` + `eng/constants.mjs` in
`github/awesome-copilot` (MIT). Batch them into one pass.

- **Description floor.** We enforce ceilings (description <= 500, frontmatter
  total <= 1024) and no floor. Upstream uses
  `SKILL_DESCRIPTION_MIN_LENGTH = 10`. A one-word description passes every check
  we have today while being useless for routing — and routing quality is the
  thing we measure hardest.
- **Bundled asset size cap.** Upstream refuses any bundled asset over 5 MB. We
  check no sizes at all, and docket ships a whole `server/` tree that the
  marketplace has to carry.
- **Asset directory allow-list.** Upstream only walks `references/`, `assets/`,
  `scripts/` inside a skill. Our `references/` convention is documented in
  `toolkit-skill-standards` but unenforced, so a stray directory ships silently
  through the mirror.

Not worth a workflow on their own; good filler alongside #13, which touches the
same test surface.

Found 2026-08-09 surveying awesome-copilot's maintenance machinery.

Verification: shipped as PR #43 (squash 0adab8d), +64 lines in tests/skill-invariants.test.mjs. Two landed as proposed: description floor at 40 chars (upstream's 10 would never fire — observed min is 86, session-handoff; 40 is ~half that, so a terse trigger still fits and a stub does not) and a 5 MB bundled-asset cap (largest file in skills/ is an 88 KB eval corpus, so it is pure headroom against a binary or vendored dep reaching three install channels). The third deviated from the entry: an asset directory *allow-list* would have been wrong, since upstream permits 3 dir kinds and we use 7 (references, evals, hooks, server, lib, bin, agents). Inverted into a change detector instead — a NEW kind of directory under a skill is how the recurring blind spot starts, because hooks/ and server/ load from the plugin root where sync.sh's skill-tree copy never reaches them, and each needed its own sync step and mirror test (that was 6ba6572; hooks/ stayed hand-maintained in dist/ until 2026-08-05 regardless). The failure message asks whether the new dir is also a new out-of-tree mirror target before it gets added to the set. Each check was verified to fail before passing: skills/git-commit/scratch/, a 6 MB file, and a 19-char description each produce their own error. Verified: node --test 213 -> 215 green, sync.sh clean, check-supply-chain.mjs clean over 159 files; CI, Plugin Release, and Release all green on 0adab8d, no plugin release cut (tests/ only, no version bump).

## 9. INSTALL.md front door for condux — consolidate three scattered installers (split from #5, 2026-08-06) (2026-08-06) — ✅ DONE 2026-08-10

condux is the larger half of #5, and the reason is not obvious from the parent
item: condux has no plugin-level installer, but it already ships two, buried
inside skills where no user would find them.

| existing | does |
|---|---|
| `skills/plan-review/references/install-codex-hook.sh` | merges the Stop hook into `hooks.json`, enables `features.hooks` |
| `skills/subagent-execution/references/install-codex-agents.mjs` | installs the four specialist agents for Codex |
| *(no script)* | OpenCode: add `plugin: ["@jabworks/condux"]` to `opencode.json` |

Per host:

- Claude Code — nothing to do; the plugin manifest registers the hooks. Report
  `skipped`.
- Codex — both scripts above, plus the experimental hooks feature flag.
- OpenCode — one JSON key.

So this is a **front door over three scattered mechanisms**, not a new
installer. That carries a design decision the concord half does not: whether
those two scripts get absorbed into one installer, wrapped by it, or left in
place and merely documented by INSTALL.md. Decide that before writing code —
absorbing them moves files two skills own, wrapping them keeps the duplication
but costs nothing, and documenting alone leaves the discovery problem half
solved.

Unblocks `condux-doctor --fix`, which today prints a repair it cannot perform.

Verification: shipped in condux 2.12.0. `plugins/condux/install.mjs` + `INSTALL.md` — detect → register → verify → report, flags `--host`, `--dry-run`, `--uninstall`; `condux-doctor` gains `--fix` (delegating to that installer, never reimplementing registration) and a Codex `features.hooks` probe. The design decision the entry flagged resolved to **wrap, not absorb**, on evidence rather than taste: `plugins/` never reaches the `npx skills add` tree, so absorbing the two sub-installers would have deleted them from the one channel where they are the sole mechanism. Both stay in `plan-review` and `subagent-execution`, invoked in place — they are position-dependent (`$SCRIPT_DIR/annotate-server.js`, `../agents`).

Two corrections the entry did not anticipate. (1) Its host table was stale: the Codex Stop hook ships in `hooks/codex-hooks.json`, so `install-codex-hook.sh` is redundant for a plugin install and load-bearing only on npx — the installer branches on whether a plugin manifest is present. (2) A live false-green: nothing in the plugin can enable Codex's experimental `features.hooks`, and the doctor never read it, so a plugin install with the flag off reported the Codex row `done` while both hooks were inert. Proven by running old and new doctors against byte-identical state (`done` vs `broken`), not asserted. Folded in rather than split.

Also corrected while implementing: `doctor.mjs` did not honour `$CODEX_HOME` though both sub-installers do, so the installer's verify beat could pass against a different directory than it had just written to. And `--uninstall` deliberately leaves `features.hooks` set — concord and plan-review ride the same flag.

Verification: node --test 215 → 223 (8 new: 3 doctor, 4 installer safety, 1 plugin-level mirror), each triggered to fail before passing. sync idempotent with no drift; frontmatter canonical 119 files; supply-chain clean 159 files; manifests paired at 2.12.0 and registered. Sandbox-verified end to end: dry-run writes nothing, re-runs duplicate nothing, malformed `opencode.json` and a duplicate `[features]` table are refused with the user's bytes intact, `--fix` repairs a broken Codex row to `done`. Code review raised one Important finding, fixed in branch: `sync_plugin_files` globbed without `dotglob` while its mirror test read the directory with `readdirSync`, so a plugin-level dotfile would have been demanded by the test and never copied. Known gap, deliberately not fixed here: `scripts/check-supply-chain.mjs` scans `skills/` only, and this ships the repo's first plugin-level executable.

## 20. Review and redesign the shipped HTML templates, navigation first (2026-08-12) — ✅ DONE 2026-08-12

Every plugin that renders HTML built its own from scratch. There is no shared
stylesheet, no shared shell, and no shared navigation pattern — four templates,
four answers, and the answers disagree about the basics.

| Surface | Lines | nav | TOC | search | sticky |
|---|---|---|---|---|---|
| `plan-review/references/plan-review-template.html` | 732 | yes | yes | yes | no |
| `record/server/docket-render.mjs` | 266 | no | minimal | yes | yes |
| `session-report/template.html` | 1442 | no | no | one | yes |
| `session-handoff/references/handoff-template.html` | 603 | no | no | no | no |

Only `prefers-color-scheme` is common to all four, and only because each
reimplemented it. The largest document by far (session-report, 1442 lines) has
the weakest navigation of the set.

**spec-browser is a different problem than it looks.** It renders no HTML at
all — `references/build-index.js` writes a markdown `index.md` catalog and
nothing else. Browsing a specs tree in a browser goes through plan-review's
directory mode (`annotate-server.js <spec-dir>`, `DIR_MODE`), which was built to
*review one plan* and was extended to walk a folder. So "spec-browser navigation
is hard" is really "the spec doc-site is a plan reviewer wearing a hat", and the
fix is a design decision about whether spec browsing gets its own surface or
plan-review's directory mode grows into one properly.

Decide before building:

- One shared shell (header, nav, theme, search) that all four render into, or
  four templates that merely agree on a navigation contract? The hard constraint
  is that every artifact must stay **self-contained with no egress** — no CDN,
  no external font, asserted by the supply-chain checker — so "shared" means a
  build-time include, not a runtime import.
- Does spec-browser get a real doc-site renderer, or does plan-review's
  `DIR_MODE` become one? They currently overlap and neither owns it.
- What navigation actually fits each: a long single document (handoff,
  session-report) wants a sticky TOC; a board (docket) wants filter + jump; a
  multi-file tree (specs) wants a sidebar. One pattern will not serve all three,
  which is why "add a TOC everywhere" is not the answer.

Worth pairing with #4 (the zero-count chip in `docket-render.mjs`) — same file,
and no reason to touch that renderer twice.

Raised 2026-08-12: navigation is hard in practice on spec-browser and docket.

Verification: split into #21 (token core + checker + docket conversion, absorbing #4), #22 (spec-site nav in DIR_MODE) and #23 (long-document nav) after a measurement pass falsified the entry's premise; design signed off, now committed at specs/dir-mode-navigation/design.md; no code shipped under this id. Five factual errors corrected in the successors: plan-review has no search (entry said yes), session-report has no search (entry said one), docket has no TOC (entry said minimal), three of four surfaces already share a 32-token core (entry said only prefers-color-scheme was common), and the spec-browser framing was backwards — DIR_MODE is a doc-site server that grew a verdict strip. Adjacent defect filed as #24.

## 21. Token core, checker with --fix, and docket palette conversion (split from #20, 2026-08-12) (2026-08-12) — ✅ DONE 2026-08-12

The shared visual layer for the four HTML surfaces, plus the #4 chip fix in the
same file. Design signed off 2026-08-12, now committed at
`specs/dir-mode-navigation/design.md`.

Measured, not assumed: `session-report/template.html` and
`session-handoff/references/handoff-template.html` define **identical** 56-token
sets. `plan-review/references/plan-review-template.html` shares 32 of them and
adds 3 of its own (`--hl`, `--hl-active`, `--radius-md`). Only
`record/server/docket-render.mjs` speaks a different dialect — 7 tokens,
`--bg/--fg/--line/--chip`, no name overlap. So the shared unit is a **32-token
core** with declared per-surface extensions, not one block: inlining all 56 into
plan-review ships 24 dead tokens, inlining only the core breaks `--hl` and
`--green`.

**Mechanism: a checker with `--fix`, not a generator.** The canonical core lives
in one file; `scripts/check-tokens.mjs` asserts each surface's marker-delimited
region matches, `--fix` rewrites it, a test guards it, and it gates `sync.sh`
the way `check-frontmatter.mjs` already does (`sync.sh:30-34` pre-build,
`:169-176` post-build).

The distinction is load-bearing. A generator makes the marked region build
output, so hand-editing it is a bug and `skills/` becomes partly generated —
against "edit `skills/`, it is the source of truth". A checker leaves the region
authored and merely required to match. It is also a **gate**, not a copy arm, so
it adds nothing to the surface #11 is filed against, whose complaint is about
copy decisions (bundle membership probed from `dist/`, hardcoded name→dest
checks).

Converting docket is not find-and-replace, twice over:

- `--muted` means the opposite thing in the two vocabularies. docket's
  `--muted: #6a6f76` is a muted *foreground* (used as `color:` on `.stats`); the
  core's `--muted: #222221` is a surface, with `--muted-foreground` for text. A
  mechanical rename silently inverts the role.
- The three document surfaces are dark-base with a `prefers-color-scheme: light`
  override; docket is light-base with a `dark` override. Converting flips the
  no-preference fallback. Near-invisible in practice, but it is a change.

The `--fix` path rewrites marker-delimited regions only, never code structure —
it edits a CSS block inside a JS template literal in `docket-render.mjs`.

Touches four plugins across three release channels: version bumps on condux,
docket, session-report and session-handoff, and `pnpm changeset` for the npm
channel because `packages/condux-opencode/` carries plan-review's template.
Runs through the toolkit-change-control gate.

Closes #4 in the same change — the zero-count chip is a one-liner in
`docket-render.mjs`, and there is no reason to touch that renderer twice.

Verification: Merged as 80d46ac (PR #54), released condux--v2.14.1, docket--v0.5.0, session-report--v1.6.5, session-handoff--v1.6.8. 32-token core in scripts/tokens/core.css, byte-checked in all four surfaces by scripts/check-tokens.mjs, gating sync.sh and pre-commit; both failure paths triggered by mutation. Census proves no token lost (35→35, 58→58, 58→58, 7→33) — including --gold-9/--gold-10, which the first extension pass would have deleted because the census regex skipped digit-bearing names. Adopting the core regressed docket light-theme muted text 4.81→3.32:1; fixed at the core (--muted-foreground #726c5e), repairing all four surfaces to AA from one edit. node --test 244 → 256.

## 4. Board cosmetic: hide the count chip on zero-count sections (2026-08-05) — ✅ DONE 2026-08-12

Zero-count sections render an empty chip pill after the heading (`count || empty-string` still emits the span). One-line fix in docket-render.mjs. Seen at live verification 2026-08-05.

Verification: Shipped inside #21 (80d46ac). Zero-count sections emit no chip span at all; confirmed in the live DOM — Loose threads renders no <span> while 1/9/12 still do.

## 25. Tag-based filtering on the board — the other half of category filtering (2026-08-12) (2026-08-12) — ✅ DONE 2026-08-12

Section scoping shipped with #21: chips for All / Committed / Someday / Loose
threads / Archive, plus free-text search over id, title and body. What is still
missing is filtering by *topic* rather than by workflow state.

The docket format carries no tag field — `parseOpen` yields
`{ id, idPart, title, section }` and body lines, nothing else. Three routes were
considered when this came up:

| route | cost | verdict |
|---|---|---|
| sections as categories | free | shipped in #21 — but sections are workflow state, not topic |
| `#tag` convention in titles | zero code to start | **recommended** |
| a real tags field | MEDIUM–LARGE | touches the format contract, parser, CLI, MCP server, renderer *and* the archive |

The middle route is the ladder doctrine (#6) applied to this: a `#tag` in the
title line is a rung-1 file convention. It needs no parser change, degrades to
plain text anywhere that does not know about it, and the board's existing
free-text filter already matches it today — type `#ui` and it works, because
`data-search` indexes the title. A chip row that discovers the tag set by
scanning titles then becomes a pure rendering enhancement over data that already
exists, with no format migration and no archive rewrite.

Decide before building: whether tag chips combine with section chips as AND
(scope ∩ tag) or replace them, and whether an unknown tag typed into the search
box should be offered as a chip.

Raised 2026-08-12 while shipping #21's section scopes.
#### Status 2026-08-12 — implemented alongside #21

Shipped as the `#tag`-in-title convention with a discovered tag row: pills
appear only when a docket actually uses tags, counts are faceted against the
active scope and search, and the leading-letter rule keeps id cross-references
like "split from #2" out of the tag set. Tag chips AND with section chips, as
the entry proposed. Awaiting merge with #21.

Verification: Shipped inside #21 (80d46ac). Tags are a file convention: #tag in an item title, discovered at render, no parser or format change — a docket that never adopts it renders no tag row. The leading-letter rule keeps id cross-references ('split from #2') out of the tag set, asserted by test. Tag chips AND with section chips as the entry proposed; scope and tag counts are faceted so neither row discounts itself. Verified live on a tagged demo docket in both themes.

## 24. plan-review docs say directory mode lists top-level *.md; the code walks recursively (2026-08-12) (2026-08-12) — ✅ DONE 2026-08-13

`plan-review/SKILL.md:49` and `:101` both describe directory mode as listing
"every top-level `*.md`". `annotate-server.js:96` (`listDocs()`) walks the whole
tree — its own comment says "every *.md in the tree" — and the client groups
sub-folder docs and resolves subdirectory links.

The code outgrew the prose. Two-line doc fix; matters because #22 depends on
directory mode being understood as a recursive doc-site, and the SKILL.md is
what an agent reads first.

Found 2026-08-12 verifying #20's claims against the files.

Verification: Prose fixed to match listDocs(): directory mode reviews every *.md in the tree, grouped by folder. Shipped in condux 2.14.2.

## 22. Spec-site navigation in plan-review's DIR_MODE (split from #20, 2026-08-12) (2026-08-12) — ✅ DONE 2026-08-13

The pain that raised #20: navigation is hard in practice when browsing a specs
tree. Design signed off 2026-08-12 settled *who owns the surface*; what the
navigation should become is still undesigned.

Ownership was already ratified before #20 was written.
`technical-spec/SKILL.md:100-107` routes spec preview to plan-review's annotate
server in directory mode, and `plan-review/SKILL.md:53` records that
technical-spec's own preview server was **retired into it**.

#20's framing — "the spec doc-site is a plan reviewer wearing a hat" — is
backwards. `listDocs()` (`annotate-server.js:96`) walks every `*.md` in the tree
grouped by folder; the client builds a folder-grouped sidebar with per-doc TOC;
cross-doc relative links resolve specifically so a spec catalog's `index.md`
renders — the very file `spec-browser/references/build-index.js` generates. It
is a doc-site server that grew a verdict strip, not the reverse.

So: invest here, and spec-browser stays rung 1 of the dependency ladder — the
agent-readable `index.md` catalog, working with zero servers and zero condux.
Giving it its own renderer would create a fifth HTML surface, the disease #20
diagnoses. The cross-reference between them stays descriptive, never a
dependency.

Needs its own discovery before planning: whether the gap is a sidebar redesign,
a search index, or only grouping and ordering fixes is undecided.

Verification: Shipped in condux 2.15.0 (PR #59, 03caa5b): DIRMODE doc-site tree — collapsible folders, filter over docs+headings, breadcrumb, prev/next, badge rollup. Live-verified on the 134-doc terminus tree. Discovery ratified: gap was the flat sidebar, fixed in plan-review's template; spec-browser stays rung 1.

## 23. Long-document navigation for session-report and session-handoff (split from #20, 2026-08-12) (2026-08-12) — ✅ DONE 2026-08-13

The two long single documents have the weakest navigation relative to their
size. Design signed off 2026-08-12; the pattern is agreed, the work is not
scoped.

`session-report/template.html` is 1442 lines — the largest artifact this toolkit
ships — with no nav, no TOC, and no search. Its only keyboard interaction is
arrow-key day selection (`:1153-1161`). It is the clear first target: a sticky
TOC is the pattern that fits a long single document.

`session-handoff/references/handoff-template.html` is 603 lines with 11 anchors
and nothing else. It may need nothing at all — it is a fill-in document whose
markdown twin has no navigation either. Decide before building rather than
adding a TOC on symmetry grounds.

Explicitly not a defect list for the other two surfaces. #20's uniform
four-feature table reads as one, but "sticky: no" on plan-review's `100vh` pane
app and "TOC: no" on a fill-in handoff are correct outcomes. One navigation
pattern does not serve a long document, a board, and a multi-file tree — which
is why this is separate from #22.

Depends on #21 only for the token core; the navigation work is independent.

Verification: Shipped in session-report 1.7.0 (PR #62, ef74ae8): sticky section nav — 13 DOM-derived chips, scroll-spy, smooth jump. handoff-template consciously left nav-free (fill-in doc, markdown twin has no nav). Live-verified light+dark. Closes the #20 family: #21 (token core), #22 (DIRMODE doc-site), #23 (long-doc nav) all shipped.

## 11. Declare bundle composition as data instead of if-arms in sync.sh (2026-08-09) — ✅ DONE 2026-08-14

`scripts/sync.sh:55-114` decides what goes where using two mechanisms that both
fail quietly:

1. **Bundle membership is inferred by probing the build artifact** —
   `if [[ -d "$DIST_DIR/$p/skills/$p/$name" ]]` walks `dist/plugins/*/` looking
   for an existing directory. So `dist/` is partly its own source of truth, and
   a skill whose dist target does not exist yet prints `SKIP` and syncs nothing.
2. **Plugin-level dirs are three hardcoded name equality checks** —
   `subagent-execution -> condux/agents`, `workflow -> condux/hooks`,
   `record -> docket/server`. Each carries a comment admitting it is a repeat of
   the `6ba6572` blind spot, and `hooks/` stayed hand-maintained in `dist/`
   until 2026-08-05 anyway.

Current doctrine — "every out-of-tree mirror target needs its own sync step AND
its own test" — is a discipline fix for something that wants to be a data fix.

Upstream precedent: `github/awesome-copilot`, `eng/materialize-plugins.mjs`
(MIT). Their `plugin.json` carries a composition manifest listing repo-relative
sources (`extensions["com.github.awesome-copilot"].{agents,skills,hooks,commands}`);
the build resolves each entry, copies it, then projects the manifest down to a
spec-field allow-list so build-only fields never reach the served manifest.
Adding a new plugin-level dir there is a data edit, not a code edit plus a
remembered bespoke test.

**Do not copy the other half.** Verified from their git tree: `plugins/` holds
only `plugin.json` + `README.md` (92 of each) and **zero materialized content is
committed** — it is assembled at publish time and removed by
`clean-materialized-plugins.mjs`. Our three install channels read committed
trees (`npx skills add` <- `skills/`, marketplace <- `dist/plugins/`, OpenCode
<- `dist/opencode/`), so `dist/` stays committed and `dist-mirror.test.mjs`
keeps guarding it byte-for-byte. Only the declaration transfers.

Decide before writing code:

- where the declaration lives — inside each `plugin.json` (one file, but adds a
  non-spec field both hosts must tolerate) or a sibling `composition.json`
- whether one generic "every declared source->dest pair is mirrored, and no
  undeclared dir exists in dist" test replaces the three bespoke mirror tests
  (`condux-hooks.test.mjs`, the agents check in `skill-invariants.test.mjs`,
  `docket-server.test.mjs`) or runs alongside them

Touches every plugin manifest and `dist/`, so it runs through the
toolkit-change-control gate.

Found 2026-08-09 surveying awesome-copilot's maintenance machinery.

Verification: Shipped in 36b6406 (PR #64) — composition.json + composition.mjs; sync.sh consumes declared pairs, hard error replaces silent SKIP; generic composition.test.mjs replaces the three bespoke mirror assertions. First run of the inverse guard caught dist/plugins/docket/.mcp.json shipping with no source since 2b4024e.

## 12. Generate marketplace.json and the README catalogs instead of testing them (2026-08-09) — ✅ DONE 2026-08-14

We hand-maintain `.claude-plugin/marketplace.json` and the plugin catalogs in
`README.md` / `CLAUDE.md`, then assert completeness after the fact —
`docs-catalog.test.mjs` fails when a marketplace plugin is missing from a
catalog, and `skill-invariants.test.mjs` checks the marketplace paths resolve on
disk.

Validating catches the omission; generating makes it unrepresentable. "Missed
registration" is already a named entry in the failure ledger
(`skills/toolkit-failure-archaeology/`), and everything in those files is
derivable from `plugin.json` plus the skills tree.

Upstream precedent (MIT): `eng/generate-marketplace.mjs` builds the marketplace
from `plugins/*/plugin.json` — name, description, version, source path — sorted
case-insensitively, as part of `npm run build`. `eng/update-readme.mjs` (36 KB)
generates the README and the `docs/README.*.md` catalogs the same way; their
skills catalog is 207 KB and entirely generated.

Caveat that shapes the design: marketplace descriptions were deliberately
allowed to diverge from their SKILL.md descriptions (ratified by-design
2026-08-04, PR #16). A generator must take that divergence as declared input —
a per-plugin marketplace-description field — rather than flattening the two back
together. Getting this wrong silently reverts a ratified decision.

Sequencing note: this shares a source of truth with #11. If the composition
manifest lands first, the generator reads it; if this lands first, it will want
the same data and the two should be designed together.

Found 2026-08-09 surveying awesome-copilot's maintenance machinery.

Verification: Shipped in 36b6406 (PR #64) — marketplace.json fully generated (byte-identical at cutover; PR #16 description divergence preserved as declared input) and four catalog table blocks in README.md/CLAUDE.md regenerated between markers by scripts/generate-catalogs.mjs; docs-catalog.test.mjs retired in favor of generator-parity assertions.

## 27. Cursor compatibility — verify the skills channel, decide on a trigger-variant build (2026-08-14) — ✅ DONE 2026-08-14

README claims Cursor support via `npx skills add` (the vercel-labs CLI
auto-detects the host), but no Cursor install has ever been verified
end-to-end. Two open questions:

1. **Does the skills channel actually work on Cursor?** Install the toolkit
   into a real Cursor setup and check the skills land, load, and trigger.

2. **Does Cursor surface `when_to_use`?** Condux-style skills carry their
   trigger conditions in `when_to_use` frontmatter. OpenCode only surfaces
   `description`, which is why `dist/opencode/skills/` exists (build folds
   `when_to_use` into `description`, 1024-char cap). If Cursor has the same
   constraint, it needs the same treatment — likely a `dist/cursor/` variant
   out of `scripts/build-opencode.mjs` generalized, or reuse of the OpenCode
   tree if the caps align.

What Cursor will NOT get regardless (host-feature gaps, document rather than
build): the condux SessionStart routing hook (routing falls back to catalog
inference, ~80% in evals), plan-review's ExitPlanMode/Codex Stop hooks, named
agents. Docket's MCP server could work on Cursor via a manual mcp.json entry —
the installer only targets Claude Code/Codex/OpenCode today; the dependency-
free CLI fallback (degrade-ladder rung 2) should work as-is.

Outcome shape: a verified compatibility row in README (what works, what
degrades, what's absent), plus a build decision on the trigger variant.

Filed 2026-08-14 after the "are we compatible with cursor?" question — the
honest answer was "advertised, never tested".

Verification: Shipped in bb20013 (PR #66). Both questions answered with live evidence (Cursor on Windows + WSL remote): the skills channel works project-scoped via dist/cursor/skills (new build-cursor.mjs variant tree, guarded by cursor-dist.test.mjs) — Cursor never reads when_to_use, so the merged tree is the only correct source; global CLI installs are broken upstream (vercel-labs/skills#421) and invisible on WSL split-home. Docket installer gained a cursor target (0.6.0, released). README carries the verified compat row. Evidence: specs/cursor-channel/verification.md.

## 29. Agent Plugins spec conformance — root plugin.json for every dist plugin (2026-08-14) — ✅ DONE 2026-08-14

Cursor supports the open agent-plugins.org standard: "spec-conformant plugins with a plugin.json manifest at the plugin root, packaging skills and MCP servers... loads in Cursor without changes" (cursor.com/docs/plugins). Our dist plugins carry only .claude-plugin/ and .codex-plugin/ manifests — not conformant. Adding a root plugin.json (schema https://agent-plugins.org/schemas/1.0.0/plugin.schema.json) makes every plugin loadable in Cursor directly (~/.cursor/plugins/local, team marketplaces, cursor.com/marketplace submission) and portable to other spec hosts.

Scope: root plugin.json per dist plugin (generated, not hand-maintained — pair with the existing manifests via sync), manifest-parity coverage for the third manifest, check the spec's expectations for skills/ layout and mcp.json vs docket's .mcp.json. Hooks/rules stay out — those are Cursor-Plugin-format components.

Origin: 2026-08-14 Cursor-channel session — the docs check that corrected "Cursor reads Claude Code plugin format" (undocumented import) to "Cursor supports Agent Plugins" (documented standard).

Verification: Shipped in 8688e5b (PR #68; original PR #67 missed main by 11s — merged into its already-squashed stacked base). All 12 plugins are spec-conformant Agent Plugins: generated root plugin.json (generate-agent-manifests.mjs, byte-guarded by agent-plugins.test.mjs), bundles flattened to skills/<skill> depth one, docket spec mcp.json beside Claude's .mcp.json. Live-verified via ~/.cursor/plugins/local: plugins load as Local, bundle skills flat-discovered, docket MCP connects (4 tools); claude plugin validate --strict clean. Evidence: specs/agent-plugins-conformance/verification.md.

## 28. Cursor follow-ups — toolkit-ops channel docs, docket-doctor cursor probe (2026-08-14) — ✅ DONE 2026-08-14

The Cursor channel shipped (docket #27): dist/cursor/skills via scripts/build-cursor.mjs, cursor-dist.test.mjs, docket installer cursor target (0.6.0), README/CLAUDE.md channel docs. Deliberately scoped out, same precedent as the OpenCode channel:

1. toolkit-ops docs still describe three channels — toolkit-orientation (SKILL.md + references/porting.md), toolkit-plugin-reference, toolkit-foundry mention build-opencode/dist layout without the cursor tree. Refresh + toolkit-ops patch bump.
2. docket-doctor probes Claude/Codex/OpenCode registrations but not ~/.cursor/mcp.json — add a cursor row mirroring the installer's target.

Verification evidence for the shipped channel: specs/cursor-channel/verification.md. Upstream caveat to re-check before advertising global installs: vercel-labs/skills#421 (fix PR #464 unmerged as of 2026-08-14).

Verification: toolkit-ops docs refreshed for the Cursor channel across toolkit-orientation (SKILL.md + references/porting.md), toolkit-plugin-reference, toolkit-foundry and toolkit-change-control — three channels became four, dist/cursor + build-cursor.mjs + cursor-dist.test.mjs now documented, and porting.md's Layer 1 guidance amended by the first port made after it was written. docket-doctor gained a cursor probe (~/.cursor/mcp.json, mcpServers.docket.args[0]) mirroring install.sh's target; all four branches exercised live — valid registration returns done via a real initialize round-trip, dangling path and unparseable config return broken with exit 1, missing ~/.cursor returns absent with the WSL split-home reason. toolkit-ops 1.7.0 → 1.7.1 (docs), docket 0.7.0 → 0.8.0 (minor: new probed host). 265/265 tests pass. Residual #29 doc debt split out as #30.

## 30. Agent Plugins follow-up — toolkit-ops docs never gained the root plugin.json (docket #29) (2026-08-14) — ✅ DONE 2026-08-14

Sibling debt to #28, found while closing it. The Agent Plugins conformance work
(#29, PR #68) added a THIRD manifest per plugin — the generated root
`plugin.json` (agent-plugins.org 1.0.0, from
`scripts/generate-agent-manifests.mjs`) plus docket's spec-dialect `mcp.json` —
and the toolkit-ops docs never absorbed it.

Concretely:

1. `toolkit-plugin-reference` is *the* manifest schema reference and documents
   only the `.claude-plugin` / `.codex-plugin` pair. It has no entry for the
   root `plugin.json`: not its closed schema, not that it is generated (never
   hand-edited), not the `${PLUGIN_ROOT}` vs `${CLAUDE_PLUGIN_ROOT}` split
   between docket's spec `mcp.json` and Claude's `.mcp.json`. Someone reading
   it to answer "what manifests does a plugin need" gets a two-thirds answer.
2. `toolkit-change-control` classifies changes by what they touch; a
   manifest-only fix row says "both `.{claude,codex}-plugin/plugin.json`" and
   does not mention that sync regenerates the root manifest too.

What #28 already fixed in passing (do NOT redo): the flattened bundle layout
(`dist/plugins/<bundle>/skills/<name>/`, depth one) in toolkit-orientation,
toolkit-foundry and toolkit-change-control, and the `agent-plugins` row in
foundry's test table.

Filed 2026-08-14 while closing #28.

Verification: toolkit-plugin-reference gained the two missing manifest sections: the generated root plugin.json (Agent Plugins 1.0.0 — closed field set, never-emitted list, the reported-and-ignored vs fatal severity split, and immediate-children-only skill discovery) and docket's two MCP dialects side by side (.mcp.json/${CLAUDE_PLUGIN_ROOT} vs mcp.json/${PLUGIN_ROOT}, type required only on the latter), each with its own traps. toolkit-change-control now says 'both manifests' means the hand-edited pair and that the root manifest is generated, never hand-bumped. Three stale #29 facts corrected from disk: skills path has two shapes since the flattening ('./skills' for bundles), concord's codex hooks path lost a level, 11 plugin pairs became 12. Recorded honest limit: no Agent Plugins validator was run — the closed field set holds by construction and by agent-plugins.test.mjs, not by a spec client's verdict. toolkit-ops 1.7.1 → 1.7.2. 265/265 tests pass.

## 26. Worktree operations skill — safe git worktree workflows (create, switch, prune, agent isolation) (2026-08-13) — ✅ DONE 2026-08-15

Sibling to `git-commit` / `git-operations`: a decision router for `git worktree`
— create/list/switch, prune stale trees, move work between trees, and the
undo paths. Motivation: agent hosts now lean on worktrees for isolation
(Claude Code's `EnterWorktree` / `isolation: "worktree"` agents), and the
existing `git-operations` skill doesn't cover them. Scope question to settle
at design time: standalone skill vs a section grown inside `git-operations`
(the router-with-undo-paths shape is identical; what differs is trigger
vocabulary — "worktree", "parallel checkout", "agent sandbox tree").

Filed 2026-08-13 mid-#22 (unrelated to that change).

Verification: Shipped as standalone skill + plugin git-worktree 1.0.0 (13th plugin). Scope question settled at design sign-off: standalone over a git-operations section, accepting the immutable name and permanent version line. Subject settled by surveying what exists — obra/superpowers ships using-git-worktrees as a procedure, spillwavesolutions/parallel-worktrees as scripted fan-out; neither ships the card deck this ticket assumed. Adopted superpowers' native-first doctrine because our own host makes the failure reachable: with EnterWorktree present, teaching git worktree add first would create phantom state the harness cannot see. SKILL.md is guards → native-or-fallback → verb cards with undo paths → traps (411/500 desc, 126 lines, in family with git-operations); references/worktree-map.md carries the situation→action table and the shared-vs-isolated breakdown (the stash is shared — the surprise that bites two stashing agents); references/recovery.md is symptom→cause→fix. git-operations 1.1.1: description trimmed to fit the mutual guard (25 chars headroom), Out-of-scope section now separates sibling-covered from still-excluded, and its history-rewriting misgrouping of worktrees corrected. skill-routing-contracts enforces the seam both ways; 31 eval cases test it from both sides plus negatives. toolkit-ops 1.7.3 for the invalidated manifest count. 265/265 tests, claude plugin validate --strict clean. Filed #31 for a sync.sh silent-failure found en route.

## 31. sync.sh reports "0 failed" when rsync actually fails (2026-08-15) — ✅ DONE 2026-08-15

Found 2026-08-15 building git-worktree (#26).

A brand-new standalone plugin has no `dist/plugins/<name>/skills/<name>/`
directory yet. rsync cannot create nested parents, so the skill-tree copy fails:

```
rsync: [Receiver] mkdir ".../dist/plugins/git-worktree/skills/git-worktree"
  failed: No such file or directory (2)
rsync error: error in file IO (code 11)
```

**`sync.sh` printed `done — 36 synced, 0 skipped, 0 failed` on that same run.**
The rsync exit status is not checked, so a genuine copy failure is reported as a
success. It was only caught because `node --test` failed afterwards
(`dist-mirror` + `agent-plugins`), which is the safety net working — but the
sync output actively lied first, and a session that trusted it would have
committed a plugin with no skills.

Two things to decide:

1. **Check the rsync exit status** and count it as a failure. That is the actual
   bug — the `failed` counter exists and is never incremented from this path.
2. **Create the dest dir before rsync** (`mkdir -p`), which removes the failure
   mode for new plugins entirely. `toolkit-foundry`'s scaffold step tells the
   author to `mkdir -p dist/plugins/<name>/skills/<name>/references` by hand;
   sync doing it itself makes that step unnecessary rather than load-bearing.

Both are small. (1) is the correctness fix and should land regardless; (2) is
the ergonomic one. Note the interaction: with (2) alone the symptom disappears
but the silent-failure class stays for every other rsync invocation.

Verification: copy_dir mkdir -p's the dest and returns the copy status; sync_skill and sync_plugin_files propagate it, so a failed copy is counted and the run exits non-zero. Both docket decisions landed (status check + mkdir -p), and the class is closed for sync_plugin_files' cp calls too. Regression test in tests/script-safety.test.mjs runs the real script against a throwaway repo clone under a stub rsync — verified it reproduces '36 synced, 0 skipped, 0 failed' at exit 0 on the pre-fix script. toolkit-foundry no longer tells authors to hand-create the dist skill tree; toolkit-ops 1.7.4. 266/266.

## 34. Specs and the docket archive cite .condux/ paths that are gitignored — two already dangle (2026-08-16) — ✅ DONE 2026-08-16

Found while auditing `.condux/` for prunable working state (2026-08-16).

#### The defect

Committed, durable files cite evidence living in `.condux/`, which is
gitignored working state. The citations are therefore dead for anyone who
clones this repo, and dead on any second machine.

Seven citation lines exist across five files. **Two of the six distinct targets
already point at nothing** — the plugin-doctor design and the
uninstall-convention design, cited from `specs/plugin-doctor/index.md`,
`specs/uninstall-convention/index.md` and `docket/archive/2026.md`. Both are
gone; neither is recoverable.

Four targets are still present, but only on the authoring machine: the
cursor-channel and agent-plugins-conformance verification reports (cited three
times from `docket/archive/2026.md`), the html-surface-tokens-and-navigation
design (twice, same file), and the docket design (`specs/docket/index.md`).

*(Paths deliberately not written out here — this entry is itself a durable file,
and the enforcing test below would flag it.)*

#### Why it happened

The artifact contract is sound in isolation — workflow's Artifacts table is
explicit that `specs/` is what you keep and `.condux/` is scaffolding. What is
missing is a rule about *citation direction*: durable content may not depend on
ephemeral content. Both `technical-spec` (writing "Initial spec from signed-off
design (.condux/designs/…)") and the docket's own closing entries do exactly
that, and neither skill flags it.

Note this is the same failure shape as docket #16 (supply-chain lint not
covering plugin-level files) — a link-integrity gap in a tree nothing walks.

#### Options, not yet decided

1. **Promote on cite.** When a durable file needs to reference a design or
   verification report, copy that artifact into the repo first (the specs dir
   it belongs to) and cite the committed path. Costs repo size; the reports are
   markdown and small — the 2.4M in `.condux/` is all screenshots, none cited.
2. **Stop citing.** Treat designs and verification as unciteable by
   construction: durable files may say "verified live, see PR #66" but never
   name a `.condux/` path. Cheapest, loses the trail.
3. **Summarise on close.** The docket entry or spec absorbs the *substance* of
   the evidence at close time, so the artifact becomes redundant rather than
   referenced.

#### Enforcement

Whatever is chosen, it wants a test — a walk over `specs/` and `docket/` for
`.condux/` references, failing on any hit (option 2) or on any hit that does
not resolve (option 1). Without one this silently recurs, which is how two
citations died unnoticed.

Verification: Ratified **promote-on-cite**: when a durable file needs to reference a design or verification report, the artifact is copied into the repo first (the spec dir it belongs to) and the committed path is cited.

Four live artifacts promoted out of working state and committed: specs/docket/design.md, specs/dir-mode-navigation/design.md (the whole #21/#22/#23 HTML-surface programme, of which dir-mode-navigation is the #22 half), specs/cursor-channel/verification.md, specs/agent-plugins-conformance/verification.md. All four are markdown with no image links — the screenshots stay out, as decided 2026-08-16.

The two dead targets could not be promoted. Their citations now say so plainly and name the surviving record instead (the spec itself; PR #51 for uninstall-convention).

Enforced by tests/durable-citations.test.mjs, which walks specs/, docket/, README.md and CLAUDE.md for a path bearing a file extension inside any gitignored working-state dir — condux, session-handoff, session-report, remember. The rule is durability, not condux: a citation into a sibling dir rots identically, and enforcing only the instance found would have been the same narrowness this entry was filed about. Enumerated rather than matched as a generic dotdir, since .github/workflows/… is a perfectly good committed citation. The extension requirement is deliberate: durable docs may still discuss working state as a concept (README draws the directories; this entry reasons about them) but may not point inside one. A second test pins the four promoted files, since deleting one would restore the dangling-citation state while the absence test still passed.

Grep found seven citation lines, not the six this entry claimed — the hand-compiled list missed one.

Scoped out, filed as its own entry: authoring-time prevention in technical-spec. Encoding the rule in the skill drags in a condux version bump, a pnpm changeset and a dist sync, which is the same reason #33 was filed rather than fixed.

## 33. technical-spec's scaffold writes no purpose line, so every new spec starts at "—" in the catalog (2026-08-16) — ✅ DONE 2026-08-16

Follow-up from #77 (spec-browser 1.1.1), which fixed the reading half of this.

#### The gap

`skills/technical-spec/references/scaffold.sh` writes `index.md` as a title
followed straight by a metadata block — `**Last updated:**`, `**Commit:**`,
`**Status:** draft`, then `## Contents`. There is nowhere for a one-line
purpose to go. spec-browser's catalog looks for a `>` note or an opening prose
paragraph, finds neither, and renders `—`.

#77 made that `—` *honest* — before it, the catalog reported
`**Last updated:** 2026-08-13` as the purpose of six of nine specs. But honest
is not useful: every spec scaffolded from now on still lands in the catalog
with no description until someone adds one by hand, which is exactly the step
people skip. Three of this repo's own specs needed one backfilled in #77, and
they were the oldest ones — the pattern is not hypothetical.

#### Shape of the fix

Add a `>` purpose placeholder to the scaffold template, between the title and
the metadata block.

Open question worth deciding rather than assuming: a literal placeholder would
be generated into the catalog verbatim until edited, which is arguably worse
than `—` because it looks like content. Alternatives: have the scaffold prompt
for the purpose (it already takes a name argument), or teach build-index.js to
treat a known placeholder string as absent. Decide before implementing.

#### Why it was not folded into #77

The scaffold belongs to `technical-spec`, which ships in condux — a different
plugin from spec-browser. Touching it needs its own version bump and, because
sync regenerates `packages/condux-opencode/skills/`, an npm changeset
(`pnpm changeset`), or `npm-channel.test.mjs` fails. Deliberately kept out of a
spec-browser patch.

#### Related

Nothing validates the shape of `specs/` entries. No test walks the tree, so a
malformed spec dir stays silently malformed — `specs/friction-audit-2026-07-29/`
has no `index.md` at all and is invisible to the catalog by design, which is
correct but undetected.

Verification: Resolved the entry's open question with a measurement instead of a preference. Probed every placeholder placement against build-index.js's extractor: a "> <!-- … -->" note, a bare comment above the metadata, and a comment below it all rendered verbatim into the catalog. #33's preferred fix was therefore unavailable inside condux alone — the third listed alternative (teach the reader to treat a placeholder as absent) was the only one that works, generalized from a magic string to the honest rule.

spec-browser 1.1.2: metaOf skips HTML-comment lines and strips inline ones from a purpose. An HTML comment is never a spec's purpose — it is invisible in every rendered view of the file, so promoting it into the catalog puts text on the page the spec itself does not show. That was a live defect independent of this entry, the same class as the metadata leak fixed in #77. Five regression tests: comment-as-purpose, comment-does-not-swallow-the-note-below-it, multi-line, inline-trailing, and an unterminated comment that must exhaust the scan rather than spin.

condux 2.17.0: scaffold.sh writes the prompt as an HTML comment between title and metadata, so a scaffolded spec still catalogs as an honest em dash while the author sees the instruction in the file they have open. templates.md shows the filled-in form; technical-spec's SKILL.md makes replacing it a step of writing content files. Verified end to end in a throwaway repo — scaffold, then catalog, reads "—".

Not done, and worth its own entry if it ever bites: nothing validates the shape of a specs/ entry, so a malformed spec dir stays silently malformed (carried from this entry's Related).

## 35. technical-spec writes the .condux/ citation the policy now bans, so promote-on-cite is enforced only after the fact (2026-08-16) — ✅ DONE 2026-08-16

Filed 2026-08-16 when #34 closed. `tests/durable-citations.test.mjs` catches a
banned citation at commit time; nothing stops one being written.

**Corrected 2026-08-16, before implementing.** As filed, this entry claimed
`technical-spec`'s scaffold stamps the citation into every `index.md` it
creates. It does not, and never did: `git log -S'.condux/designs' --
skills/technical-spec/` returns nothing, and both `scaffold.sh` and
`templates.md` write a bare "Initial spec" changelog line. The claim was
inferred from the shape of the citations rather than checked against the file —
the same mistake as the hand-compiled list of six in #34, which grep corrected
to seven.

What is actually true: the agent wrote those lines by hand. `discovery` saves
the design to `.condux/designs/<date>-<feature>.md` and names that path in its
own instructions, so the path is the salient thing to cite at spec-writing
time, and **nothing anywhere said not to**. The defect is a missing rule, not a
bad template.

Two halves, and they can ship separately:

1. ~~**Scaffold.** Stop emitting the `.condux/` path.~~ No-op — see the
   correction above. The scaffold was always clean; what it gains instead is a
   positive instruction, since it is the tool the author has open at the moment
   the citation gets written.
2. **Doctrine.** The artifact contract in `workflow` states the durability split
   (`specs/` keeps, `.condux/` scaffolds) but says nothing about citation
   direction — durable content may not depend on ephemeral content. That
   sentence is what was actually missing; both `technical-spec` and the docket's
   own closing ritual violated it without either skill noticing.

Deferred at close rather than folded in for the same reason as #33: both files
live in condux, so the fix needs a condux version bump *and* a `pnpm changeset`
(sync regenerates `packages/condux-opencode/skills/` from `skills/`, and
`npm-channel.test.mjs` fails without one). That turns a docs-and-test PR into a
release. Worth pairing with #33 — same skill, same scaffold file, one bump.

Related: #33 (scaffold writes no purpose line), #34 (the citations themselves),
#16 (link-integrity gap in a tree nothing walks — the same failure shape).

Verification: Closed together with #33 — same skill, same files, one condux bump.

The entry's central claim was wrong and was corrected in the body before implementing: technical-spec's scaffold never emitted the .condux/ citation. git log -S'.condux/designs' on that tree returns nothing, and both scaffold.sh and templates.md write a bare "Initial spec" line. The agent wrote every one of those citations by hand, because discovery names the design's path in its own instructions and no skill anywhere said not to cite it. So half 1 of this entry was a no-op and the defect was entirely half 2.

Shipped as doctrine, in two places. workflow's Artifacts section gains the missing rule stated as a rule — durable content may not depend on ephemeral content, a committed file may not cite a path inside .condux/, promote the artifact into the durable tree and cite the committed path, and when the artifact is already gone name what survives (a PR, a commit) rather than a path that resolves nowhere. technical-spec carries the operative half, in SKILL.md and templates.md, because it is the skill holding the pen when a spec's changelog gets written.

discovery deliberately untouched. Saving a design to .condux/designs/ is correct; citing it from a durable file is the error, and discovery does not do that. Putting the rule at every site that merely names a working-state path would spread it thinner than it is worth — all three skills ship inside condux, so one canonical statement plus one operative restatement is enough. (The restate-inline invariant in CLAUDE.md is about toolkit-ops not being installed alongside shipping skills; it does not apply within a single bundle.)

Enforcement remains after-the-fact by design: tests/durable-citations.test.mjs (from #34) is what makes the rule real, and it now walks every gitignored working-state dir rather than just .condux/. This entry adds the before-the-fact half, so the author meets the rule in the tool rather than in a red test.

## 32. subagent-deployment routes at 62.5% — four of six misses bleed to workflow (2026-08-15) — ✅ DONE 2026-08-16

Measured 2026-08-15 during the git-worktree eval run (#26), full corpus,
claude-haiku-4-5-20251001, 582 cases. Overall was 90.0% — in the established
~89-92% band. **subagent-deployment scored 10/16 = 62.5%**, the weakest skill
in the corpus by a wide margin and roughly 27pp below band.

Nothing to do with the change that surfaced it. The failure mode is coherent
rather than noisy, which is what makes it worth fixing:

| query | expected | got |
|---|---|---|
| fix these 3 unrelated failing test files at once | subagent-deployment | workflow |
| two independent bugs in different packages, handle both | subagent-deployment | workflow |
| batch these lookups together | subagent-deployment | null |
| spawn a generic subagent with this custom prompt | subagent-deployment | null |
| one small task, just do it yourself | workflow | null |
| parallelize the test suite in ci | null | workflow |

**Four of six involve `workflow`** — twice swallowing a case that should reach
the fan-out skill, once being the expected answer and losing to null, once
capturing a should-not-trigger case. That is a one-hop adjacency, exactly the
error class A3 names as dominant.

The plausible mechanism, to be checked not assumed: `workflow`'s trigger
contract claims *every* implementation request ("Trigger with any
implementation request… Every dev task starts here"), which is true as routing
doctrine but makes its description a strong attractor for any multi-task
phrasing. `subagent-deployment` then has to win against a skill that has
declared itself the universal entry point. The two do not currently name each
other in `tests/skill-routing-contracts.test.mjs` — the pairs there are
`['subagent-deployment','subagent-execution']` only.

Worth considering, in order:

1. Add `['subagent-deployment','workflow']` to the routing-contracts pairs so
   the seam is at least test-enforced in both directions.
2. Sharpen subagent-deployment's description on the *independence* signal
   (nothing shared, no ordering) rather than on the fan-out mechanics, since
   "3 unrelated files" and "two independent bugs" are the exact phrasings that
   missed.
3. Re-measure. Do not tune the corpus — two of the six ("batch these lookups
   together", "parallelize the test suite in ci") may be genuinely ambiguous
   cases worth keeping as recorded misses.

Prior art on the seam problem: docket #10 (semantic collision detection) and
#14 (trajectory-based eval) both bear on measuring exactly this.

Verification: Measured with a 3-run variance band, and the band overturned most of what a single run appeared to show.

Band: 90.2% / 91.1% / 91.4% = mean 90.9% +/- 1.5pp, 47 flaky cases out of 582. The 2026-08-15 baseline of 90.0% sits inside that band, as does the 89.5% single run taken mid-session. Nothing moved overall, and 8% of the corpus is unstable between runs — larger than any effect this entry proposed to measure.

CORRECTION TO THIS ENTRY'S PREMISE. The headline '10/16 = 62.5%, roughly 27pp below band' compared a hand count over subagent-deployment's own eval file against a band derived from overall accuracy. The 2026-08-15 report's own per-expected-skill row reads 11/15 = 73.3%. Both numbers are real and they answer different questions; the entry mixed them, so the gap it opened with was overstated.

WHAT WAS ACTUALLY WRONG: the oracle, not the skill. Three cases asserted a routing the shipped doctrine forbids. workflow's contract says every dev task starts there and other skills execute within it, not instead of it — so a cold 'fix these 3 unrelated failing test files' is the router's, and the judge was penalised for obeying the doctrine. Corrected with the rationale recorded in a note field on each case.

Two of the three now hit 3/3, stable. The third, 'parallelize the test suite in ci', hits 1/3 — it is genuinely ambiguous between workflow and null. Kept as expected:workflow anyway, because an eval should encode the correct answer rather than the one the judge finds easy. It is a recorded miss, not a win, and the mid-session claim of '3 for 3' was an artifact of measuring once.

RECOMMENDATION 1 NOT DONE. Adding ['subagent-deployment','workflow'] to the routing-contract pairs is blocked by construction: that test asserts mutual naming and no pair in it involves workflow, because workflow's contract deliberately names no downstream skill. Satisfying it would force workflow to contradict its own universal-entry claim. A one-way disambiguation in subagent-deployment does the same work honestly.

RECOMMENDATION 2 DONE, WITH NO MEASURABLE EFFECT. subagent-deployment now triggers on the fan-out being asked for rather than on the work happening to be independent. subagent-deployment sits at 10/13 before and after. The single run appeared to show it costing 'run these together, theyre totally unrelated' — the band shows that case at 2/3, so it is noise and the edit is exonerated of harm. Kept because it is doctrinally correct, not because the number moved.

THE ONE STABLE DEFECT WAS NOT THE ONE THIS ENTRY NAMED. 'spawn a generic subagent with this custom prompt' misses 0/3 — perfectly consistent, not flaky. The skill's body bans generic subagents outright, but the trigger contract never said so, so the router could not see it. Added to the contract. UNVALIDATED: no eval run since. Re-measurement filed as a follow-up.

Recommendation 3's 'do not tune the corpus' guard held for everything except cases whose expected answer contradicts shipped doctrine, which is a wrong oracle rather than a tuned one. The remaining flaky misses were left alone as the entry asked.

## 36. Re-measure the generic-subagent contract fix, and decide what to do about a corpus where 8% of cases are flaky (2026-08-16) — ✅ DONE 2026-08-17

Filed 2026-08-16 when #32 closed. Two things that entry produced but could not
finish.

#### 1. An unvalidated fix

`spawn a generic subagent with this custom prompt` missed **0/3** in the
variance band — the only perfectly stable miss in `subagent-deployment`'s
corpus. Cause found: the skill's body bans generic subagents outright ("Never a
generic subagent with an injected prompt — only condux's four named agents")
but the trigger contract never said so, so the router could not see the rule it
was being scored on. The contract now names it.

**No eval has run since that edit.** It ships on reasoning, not measurement.
One band re-run answers it; the case is stable, so even a single run is
informative here in a way it was not for the flaky cases.

#### 2. The corpus is noisier than the effects being measured

The band reported **47 flaky cases out of 582** — 8% of the corpus changes
answer between identical runs. Mean was 90.9% ± 1.5pp. Every per-case
conclusion drawn from a single run this session was wrong or unsupported:

- `run these together` looked like a regression caused by an edit; it is 2/3.
- `parallelize the test suite in ci` looked fixed; it is 1/3.
- Only `fix these 3 unrelated tests` and `two independent bugs` (3/3) and
  `spawn a generic subagent` (0/3) said anything at all.

The operational lesson is cheap and worth writing down somewhere durable: **a
single run cannot support a per-case claim.** Anything below roughly 3pp needs
a band, and per-case verdicts need the stability column, not the accuracy
number.

The deeper question is what a flaky case *is*. Three candidate readings, and
they want different responses:

1. **Judge variance** — same prompt, different answer, nothing to fix but
   sample size. Prior art: A3 recorded 44 flaky cases; this run found 47, so
   the rate is stable across months and skill-set changes.
2. **Genuinely ambiguous stimulus** — two skills legitimately fit and the
   corpus forces one. `batch these lookups together` (1/3) reads this way. The
   `accept` alternates field already exists for exactly this and is underused.
3. **A real seam** the corpus is detecting intermittently. These are the
   valuable ones and they are currently indistinguishable from (1).

Deciding that is prerequisite to any future routing work, because right now
every measurement pays the full cost of a band to learn which bucket a case is
in.

Related: #14 (trajectory-based eval — `disallowed` assertions and deterministic
static grading would remove the judge from the loop entirely, which is one
answer to all of this), #10 (semantic collision detection).

#### Status 2026-08-17 — both halves measured

Re-ran the 3-run band on identical parameters to the 2026-08-16 band
(haiku-4-5, batch 12, cold-only, 582 cases), so the numbers are comparable.

**1. The fix holds.** `spawn a generic subagent with this custom prompt` went
**0/3 → 3/3** — absent from both the miss table and the flaky table, so it was
consistent across all three trials. The contract edit is no longer unmeasured.
Overall 91.6% ± 1.9pp against the prior 90.9% ± 1.5pp: overlapping CIs, no
movement claim, which is what a one-case fix should look like.

Nuance for whoever reads the report next: the per-expected-skill table is
**final-run only**, not band-wide. Its `subagent-deployment 13/13` row coexists
with `batch these lookups together` sitting at 1/3 in the same report.

**2. Flakiness is case-intrinsic, and bucket 1 is not the dominant reading.**
The entry framed A3's 44 and the 08-16 band's 47 as a stable *rate*; a stable
rate cannot separate the three buckets, but a stable *set* can. Intersecting
the two bands' flaky sets:

| | |
|---|---|
| band 08-16 flaky | 47 |
| band 08-17 flaky | 54 |
| overlap observed | **27** |
| overlap expected under pure judge variance | 4.4 |
| excess | **6.2×** |

Under judge variance the sets should be near-disjoint. Sharing 27 cases at six
times chance means flakiness is predominantly a property of specific cases —
so the 27 belong to buckets 2 and 3, both actionable, and can be triaged once
instead of costing a band each time they are re-encountered.

The 27 fall into families: two-skill stimuli that `accept` alternates already
exist for (`verify this`, `how is this repo organized`, `any security issues in
this change?`), a four-case test-routing cluster that reads as a real seam
rather than noise (test-first-development ↔ workflow), and known-hard oracle
calls carried deliberately (`parallelize the test suite in ci`, 1/3).

Triage of the 27 is refiled as its own item; the bucket question this entry
asked is answered.

Verification: 3-run band re-run on identical parameters (haiku-4-5, batch 12, cold-only, 582 cases): target case `spawn a generic subagent with this custom prompt` moved 0/3 to 3/3, stable across all three trials; overall 91.6% ± 1.9pp vs the prior 90.9% ± 1.5pp (overlapping CIs, no movement claimed). Second half answered by intersecting the two bands' flaky sets — 27 shared against 4.4 expected under pure judge variance (6.2× chance), so flakiness is case-intrinsic rather than judge variance; triage of the 27 refiled as #37.

## 37. Triage the 27 case-intrinsic flaky routing cases into accept alternates and one real seam (2026-08-17) — ✅ DONE 2026-08-17

Split from #36, 2026-08-17, once the bucket question it asked was answered.

Two 3-run bands (2026-08-16 and 2026-08-17, identical parameters) share **27
flaky cases against 4.4 expected under pure judge variance — 6.2× chance**.
Flakiness is therefore predominantly a property of specific cases, not of the
judge, which is what makes this triable at all: each of the 27 can be settled
once instead of costing a full band every time it is re-encountered.

Three families, wanting three different responses.

**A. Two skills legitimately fit — use the `accept` alternates field.** It
already exists for exactly this and is underused. Clear members: `verify this`
(preflight / finalize / live-verification), `how is this repo organized` and
`im new to this repo, give me the lay of the land` (toolkit-orientation and its
neighbours), `any security issues in this change?` (code-review /
security-review), `batch these lookups together` (1/3 in both bands).

Care needed: an `accept` alternate is a claim that both answers are
doctrinally correct, not a way to make a number go up. Anything added here
should be defensible as "either routing serves the user."

**B. A real seam — test-routing.** Four cases cluster on the
test-first-development ↔ workflow boundary: `the test is failing so just fix
the test`, `update the failing test to match the new behavior`, `add tests for
the existing legacy code`, `write e2e tests with playwright for the flow`. Four
co-located cases is not sampling noise; this is a contract question about which
skill owns test work that arrives as a plain request. Answer the boundary
first, then decide what the corpus should expect.

**C. Known-hard oracle calls, carried deliberately.** `parallelize the test
suite in ci` (1/3) was ratified as a known miss on 2026-08-16 — an eval should
encode the correct answer, not the one the judge finds easy. Leave it, and
leave the record of why.

Method note worth keeping: the discriminator here was intersecting two bands'
flaky *sets*, not comparing their flaky *rates*. The rate was stable across
months and said nothing about cause; the set overlap answered it in one
computation. Any future "is this noise or a seam" question should reach for the
same test.

Related: #14 (trajectory-based eval — deterministic static grading removes the
judge from the loop, which would dissolve family A rather than annotate it).

#### Status 2026-08-17 — family B settled, harness fixed, family A now has complete data

**Family B is closed by contract edit, not by corpus edit.** `test-first-development`
gained two clauses (condux 2.17.2, commit `6c9829b`): advisory questions about the
practice, and requests to change an existing test so it passes, both in user
phrasing. The old rule existed but was written agent-side in passive voice, so the
router could not match it against a user message.

Validated by a third 3-run band on identical parameters. Filtering to
stable→stable transitions only — the sole comparison two bands of 8% flakiness
support:

- **1 case cured:** `should I tdd ui components`, a stable 0/3 miss in *both*
  prior bands, is now 3/3. It was the corpus's most stable miss.
- **0 regressions.** No case that was 3/3 in both prior bands dropped to 0/3.
- 59 cases already disagreed between the two prior bands and cannot support a
  per-case claim in either direction.

The four other family-B cases all reached 3/3, including the two expecting
`workflow`. Weaker claims (they were flaky before), but notable in kind: **no
exclusion clause was added, and the cases that were leaking away from `workflow`
stopped leaking anyway.** Sharpening a sibling's *positive* claim did the work a
negative clause was supposed to do. Worth remembering before writing the next
"not for X" line into a contract.

Overall accuracy is uninformative as usual: 91.8% ± 3.0pp against 91.6% ± 1.9pp
and 90.9% ± 1.5pp. The band's own spread widened (93.0 / 91.9 / 90.5).

**Harness fixed first, deliberately** (commit `379c923`, report-only, `isHit`
untouched so all three bands stay comparable):

1. Every run's answer is kept, not just the final run's. The flaky table gained a
   `got when missed` column. Without this, family A was unanswerable — a case that
   hit on the last run hid its own miss target.
2. Out-of-catalog answers are flagged. The judge sometimes names a Claude Code
   built-in it was never shown (`security-review`, `dataviz`, `simplify`). Those
   scored as misses while being contamination, not contract defects. A 24-case
   smoke run surfaced `simplify` immediately — a case hand-reading the final-run
   snapshots had missed.

**Family A's premise, corrected.** On 17-of-27 partial data it looked like most
flaky cases miss to `null`, where an `accept` alternate is meaningless. Complete
per-run data says the opposite: of **79** imperfect cases, **34** miss only to
null and **45** miss to a real sibling. So the triage is larger than #37 assumed,
not smaller — and it is real per-case doctrinal work, since an observed sibling is
not automatically a defensible one (`what did the 2026-07-08 audit leave open` →
`remember` is simply wrong).

One case to weigh first: `fix this failing test right now` (expected
toolkit-debugging-playbook, accepts workflow + root-cause-analysis) answered
`test-first-development` in one run. That is a direct consequence of the new
clause and is arguably correct — it *is* a request to fix a failing test. Adding
TFD to its accepts is defensible; doing it because the number moved is not.

#### Status 2026-08-17 — family A triaged: 19 accepts, 1 oracle flip, ~24 deliberate misses

Judged all 45 sibling-miss cases under two ratified rules: `workflow` chosen
over a downstream condux skill stays a miss (the eval's drift signal), and
expected-null cases are judged on the answering skill's shipped contract.

Shipped as commit `e5403d1` — 19 `accept` alternates and one oracle flip
(`publish my npm package`: null → release, whose contract covers npm publishing
outright; same class as the #32 corrections), every edit carrying a `note`.
`fix this failing test right now` got its TFD accept on the merits argued
above. Bumps: condux 2.17.3, toolkit-ops 1.7.5, adapting-skills 1.4.1,
release 1.3.1, plus a changeset.

The ~24 deliberate misses sort into contract gaps and over-trigger evidence,
recorded as #39 rather than fixed mid-pass (the pass was corpus-only by
decision). Remaining before this item can close: one validation band, read
under the stability filter — the accepts change what counts as a hit, so the
new baseline supersedes both 08-16/08-17 bands and the tfd-contract band.

Verification: Validation band (3 runs, identical parameters): new baseline 94.4% ± 0.7pp, flaky 47→41, zero stable 3/3→0/3 regressions vs the prior band. 13 of the 20 edited cases now 3/3; the 7 partials all show the accept firing where its sibling was chosen, with the residual misses going to null or to different siblings — the flaky tail, not a failed edit. Family B holds a third band (both TFD cases 3/3). The deliberate misses stayed misses: workflow-drift signal intact, out-of-catalog steady at 2 (security-review, dataviz). Residue tracked as #39 (contract gaps) and #14 (trajectory eval).

## 39. Contract gaps surfaced by the #37 triage — cases left as misses deliberately, each naming a weak seam (2026-08-17) — ✅ DONE 2026-08-18

Fixed four of the five clusters this entry named; the fifth (git-operations'
enumerated list) was a decide-once question, decided as "leave alone."

**root-cause-analysis** now claims declarative bug reports, not just
questions — "checkout crashes on empty cart" reads as a bug report even with
no question mark. Mirrors the passive-voice-to-user-phrasing fix that closed
test-first-development's half of #37 (condux 2.17.2).

**subagent-execution**'s when_to_use now names model selection for a
dispatched agent explicitly — the skill already owned this in
`references/spawn-rules.md`, the contract just never said so.

**remember** (Concord) no longer reads as covering open-ended retrospective
questions. Its contract now distinguishes "what did we do last time" (its own
session tiers can answer) from "what mistakes did past sessions make" / "what
did the &lt;date&gt; audit leave open" / "has this happened before" in an
unrelated project — a session log is not a mistake ledger or audit index; a
toolkit mistake in this project is `toolkit-failure-archaeology`, and there is
no skill for the rest.

**"sdd the plan"**, the eval stimulus ambiguous between spec-driven
development and subagent-driven execution, was reworded to "spawn the agents
for this plan" rather than resolved in either skill's contract.

**git-operations' enumerated situation list**
(undo/discard/park/integrate/recover) — left unchanged, by decision, not
oversight. The skill's own "Out of scope" section already excludes submodules
and bisect by name, pending a dedicated history-rewriting skill; broadening
the trigger contract without also rewriting the body to cover them would have
created a contract the skill couldn't honor. Confirmed with the repo owner
2026-08-18: the miss is an intentional guard.

Verification: `node scripts/check-frontmatter.mjs` clean on all three edited
SKILL.md files, well under the 500/1024 char budgets; `node --test` 281/282
(the one failure is a pre-existing stale local git hook, unrelated to this
diff); dist/opencode/cursor/condux mirrors synced and verified via `git
status`. Shipped as a condux minor bump, changeset
`contract-gaps-37-triage.md`.

Related: #37 (the triage that filed this), #14 (a trajectory eval would grade
several of these on behavior instead of a routing token).

## 38. OpenCode has no routing enforcement — condux ships the SessionStart hook to Claude and Codex only (2026-08-17) — ✅ DONE 2026-08-18

Shipped the candidate mechanism this entry drafted, after clearing both items
it flagged as needing verification before being treated as a fix.

**Timing, verified empirically, not reasoned about.** Set up a live OpenCode
install (v1.14.48) with the real `@jabworks/condux` plugin loaded from a local
path and ran `opencode debug config`: `config.instructions` in the fully
resolved config included the pushed `routing.md` path, mirroring the
already-proven `skills.paths` registration. A differential check (`--pure`,
plugin hook not run) confirmed the `instructions` key doesn't even exist
without the mutation — so this is not the `tools`→`permission` trap (that
field is folded into `permission` before the config hook runs, silently
dropping any hook-side write; `instructions` has no equivalent folding step).

**Cost/channel tradeoff, decided, not assumed.** `SessionStart` fires once per
session start/clear/compact; `instructions` is ambient — re-injected every
turn, permanently, in the same channel as the user's own AGENTS.md
(~390 tokens/turn for `routing.md`). Confirmed with the repo owner
2026-08-18: ship it anyway, for routing-enforcement parity with Claude Code
and Codex.

`packages/condux-opencode/index.js`'s `config` hook now pushes `routing.md`
onto `cfg.instructions`, guarded by `existsSync` and deduped the same way
`skills.paths` is. `condux-doctor`'s OpenCode probe now also checks that the
installed package ships `skills/workflow/hooks/routing.md`, so a broken
install reports broken instead of silently missing the enforcement.

Verification: end-to-end check against the real plugin (not just a synthetic
test plugin) confirmed the resolved config; `node --test` 281/282 (the same
pre-existing, unrelated stale local git hook failure); `node --check` clean
on both edited JS files. Shipped as a condux minor bump, changeset
`opencode-workflow-routing.md`.

## 42. Routing hook out-competes test-first-development on its own trigger phrases (7 cases at 2/3 flaky) (2026-08-20) — ✅ DONE 2026-08-20

Surfaced by the 3-run trigger eval on 2026-08-20
(`skills/toolkit-research-frontier/references/eval-blueprint-2026-08-20.md`).
Pre-existing, unrelated to blueprint.

Seven `test-first-development` cases sit at **2/3 flaky, every one losing to
`workflow`** — a coherent pattern, not scatter:

- tdd this feature
- test-first for the parser module
- write failing tests before implementing
- red green refactor cycle please
- can you do this test driven
- write unit tests for the date helpers before you code them
- bug: prove it with a failing test then fix it
- tets first plz (typo case)

**Hypothesis:** the SessionStart routing payload's "every implementation request
starts at `/condux:workflow`" claim is absolute enough that the judge routes an
explicit opt-in skill's own trigger phrases to the router instead. That is
arguably *correct* runtime behaviour — workflow would then load
test-first-development at the right checkpoint — in which case the fix is the
oracle (`accept: ["workflow"]`, the idiom `adapting-skills` and now `blueprint`
use), not the description.

Decide which before changing anything: if the intent is that these phrases fire
the skill directly, the routing payload needs an explicit carve-out; if the
intent is that workflow loads it, eight oracles need `accept` alternates and the
measured accuracy rises without any behaviour change.

Same question likely applies to the `coding-directive` pair at 2/3 → null.

Verification: Root cause was wrong oracles, not a routing defect: routing.md lists test-first-development among the skills that execute within workflow, never instead of it, so `workflow` is a correct answer for an implementation request carrying a test-first modifier. Nine cases now carry accept alternates encoded by request shape (not by observed flakiness, which would overfit to model variance); four stay strict so the corpus keeps its discriminating power. The coding-directive pair was a separate reach gap — missing to null — fixed by restructuring its triggers into when_to_use rather than trimming load-bearing disambiguation clauses out of a 484/500-char description. Verified across three 3-run evals: coding-directive 2-flaky -> 9/9, adapting-skills 11/11 and code-review 19/19 unharmed, toolkit-failure-archaeology 14/17 -> 15/17, overall mean unchanged within noise (93.4±2.8 -> 92.8±1.0 -> 93.3±2.2). One residual: "why is grep -P banned here" sits at 2/3, but its miss moved from coding-directive to null, so the collision is resolved and what remains is a tfa reach gap, not an over-reach. Reports in skills/toolkit-research-frontier/references/eval-{blueprint,oracle-rerun,oracle-verify}-2026-08-20.md. Ships via PR #87; the oracle-writing rule is now documented in toolkit-foundry.

## 45. Redesign the docket board as a real board — columns, not a stacked list (2026-08-21) (2026-08-21) — ✅ DONE 2026-08-21

The board renders sections as vertically stacked `<section>` blocks with an `h2`
each, so "Committed / Someday / Loose threads" read as a long scroll rather than
as parallel buckets. Scope pills filter to one section at a time, which is a
workaround for not being able to see them side by side. Wanted: something closer
to Trello or Jira — a column per section, items as cards — without the weight of
either.

**Decide before building — the write path is the whole question.** Trello and
Jira are *editing* UIs; docket's board is a *rendered view* of a markdown file.
`docket browse` writes a standalone HTML file that is opened later with no server
(quirks Q9), so a card dragged in that file has nowhere to persist to. Three
options, and they are meaningfully different products:

1. **Read-only columns.** Pure layout change, works in both the written file and
   `--serve`. Cheapest, and probably most of the value — seeing the buckets side
   by side is the actual ask.
2. **Drag to move, `--serve` only.** Needs a write-back endpoint that edits
   `DOCKET.md`'s section headings, plus conflict handling if the file changed on
   disk. The written-out file would then behave differently from the served one,
   which is a real trap.
3. **Drag in both, written file degrades.** Worst of both — the same artifact
   silently loses a capability depending on how it was opened.

Recommendation is (1) unless moving items is specifically what is wanted.

Other things a column layout forces a decision on:

- Section count is user-defined in `DOCKET.md`, so column count is unbounded.
  Needs horizontal scroll, or a rule for collapsing empties (see #44 — a section
  with no items currently still renders its heading).
- The archive is 35 items against 7 open; it cannot be a peer column.
- Long item bodies are the board's actual content — a Trello card shows a title
  and hides the body, which is a different reading model from today's, where the
  full body is visible inline. That is a content decision, not a layout one.
- Narrow viewports have no columns to give; the mobile form is the current
  stacked list, so both layouts have to exist regardless.

Related: #44 (empty sections), and the Surface Kit scale is already in place, so
this is layout work in `board-shell.html` plus markup from `docket-render.mjs`.

Verification: Shipped as docket 0.10.0 (PR #95, re-land of #93; commit 0dee7dd). Read-only columns per section, lede-first cards with a details fold, one-row header, archive drawer. Live-verified light/dark/420px; signed-off render at specs/docket/board-direction-a.html.

## 44. Docket board renders a section header for a section with no items (2026-08-20) (2026-08-20) — ✅ DONE 2026-08-21

A DOCKET.md section with zero open items still emits its `<h2>` and rule on the
board — "COMMITTED" currently renders as a heading over nothing. The section nav
also links to it, so the link is a jump to an empty region.

`renderHtml` maps every `open.sections` entry unconditionally
(`skills/record/server/docket-render.mjs`), and `sectionProse` already returns
`''` for a section with no loose prose — so the empty case is representable, just
not suppressed.

Not simply "skip empty sections": a section heading with a zero count is arguably
correct signal on a backlog board (it says the bucket exists and is clear), which
is why the scope pills already render `data-total="0"` at reduced opacity rather
than disappearing. Decide which reading is wanted before changing it — and if
sections are suppressed, `sectionNav()` must drop the matching link in the same
change or the nav points at nothing.

Found 2026-08-20 while visually verifying the Surface Kit redesign.

Verification: Closed by decision with #45 (docket 0.10.0, PR #95): a zero-count section renders as a quiet dashed slot on purpose — an empty lane is information, never hidden. A filter that empties a populated column shows its own no-match slot.

## 46. session-report charts use one colour for every series (2026-08-21) — ✅ DONE 2026-08-21

With several projects in view the bars are indistinguishable — you can only tell
series apart by reading the row label.

**The cause is not duplicate colours; it is that there is no palette.** There is
no `PALETTE`, no colour array and no hue rotation anywhere in
`skills/session-report/template.html`. Every bar in every chart is the same
`█`/`░` glyph run in `var(--clay)` (template.html:2097 and :2339). One colour,
reused for everything.

**This is a design-system change, not a template tweak.** The core
(`scripts/tokens/core.css`) carries no categorical colours at all — only
semantics (`--success`, `--warning`, `--destructive`, `--info`) plus the clay
accent. Reaching for the semantic ones would be wrong: semantic colour means
good / bad / attention, and a project is none of those. A categorical ramp has to
be added to the core as its own group, distinct from both the accent and the
semantics, and it then belongs to every surface, not just this one.

Constraints that shape it:

- **No egress**, so no charting library — whatever ships is CSS plus the existing
  glyph runs, or hand-authored inline SVG.
- **Both themes.** Categorical hues need to stay distinguishable on `#111110`
  and on `#f6f5ef`, which is the part that usually breaks — a ramp tuned on dark
  goes muddy on cream.
- **Accessibility.** Categorical series should not rely on hue alone; the glyph
  runs could vary pattern as well as colour, which is cheap here because they are
  text.
- **Unbounded series count.** Project count is whatever the transcripts contain,
  so the ramp needs a defined cycle-and-degrade rule rather than assuming it
  never runs out.

The `dataviz` skill covers exactly this (categorical palette construction, the
separation of semantic from categorical colour, and a runnable validator) and
should be read before picking values.

Found 2026-08-21 looking at a real report with three projects; the problem gets
worse the more projects are in range.

Verification: Shipped in #97 (90a08dd). --cat-1 … --cat-8 plus --cat-other added to scripts/tokens/core.css as their own group, computed against both surfaces rather than picked: worst adjacent CVD dE 15.8 dark / 15.7 light against a target of 8, and all eight clear 3:1 on both grounds. Both charts read one global ranking of by_project, so colour follows the project rather than its rank in the current view; slots 9-16 repeat the hues with a second channel and everything past 16 folds to --cat-other. The premise needed correcting first: a palette already existed, private to the timeline IIFE, dark-only and cycled, which is why the bars could not reach it. Released as session-report 1.10.0. Design decisions and the three traps found on the way are in specs/surface-kit (D7, Q10-Q12), with the signed-off specimen at specs/surface-kit/ramp-direction-c.html.

## 49. annotate-server directory-mode test races the feedback file write (2026-08-22) — ✅ DONE 2026-08-23

`tests/annotate-server.test.mjs:54` — "annotate-server directory mode: doc manifest, per-doc content, grouped feedback" fails intermittently:

```
AssertionError: The input did not match /\*\*Decision:\*\* Request Revisions/. Input: ''
```

**Not flakiness — a real race, and the empty string is the tell.** The file existed and was empty, so this is not a missing write or a timeout.

`annotate-server.js:265` writes with `fs.writeFileSync(feedbackFile, md)`, which is `open(O_WRONLY|O_CREAT|O_TRUNC)` followed by `write()` — two syscalls, in a different process. The test waits on **existence** and then reads once:

```js
const deadline = Date.now() + 2000;
while (!fs.existsSync(feedbackFile) && Date.now() < deadline) {
  await new Promise((r) => setTimeout(r, 50));
}
const feedback = fs.readFileSync(feedbackFile, 'utf8');
```

A read landing between the server's `open` and its `write` sees a created, truncated, still-empty file. `existsSync` is satisfied the instant `O_CREAT` lands, so the loop exits early and the single read returns `''`.

**Reproduction conditions:** observed 3 failures in ~12 full-suite runs on 2026-08-22, every one while local preview servers (`python3 -m http.server`) and Chrome were competing for CPU; zero failures in nine runs on an otherwise idle machine, and 3/3 passes when run in isolation. CPU contention widens the open→write window, which is why it looks load-correlated.

**Fix — wait for content, not for existence:**

```js
let feedback = '';
const deadline = Date.now() + 2000;
while (Date.now() < deadline) {
  try { feedback = fs.readFileSync(feedbackFile, 'utf8'); } catch {}
  if (feedback.includes('**Decision:**')) break;
  await new Promise((r) => setTimeout(r, 50));
}
```

The `try/catch` also removes the second latent bug in the current code: if the deadline expires before the file is created at all, `readFileSync` throws `ENOENT` and the test fails with a stack trace instead of a useful assertion message.

Worth checking whether the single-file mode test (`annotate-server.js:279`, same `writeFileSync` shape) has the same wait pattern.

An alternative worth weighing: have the server write to a temp file and `rename()` it into place, which is atomic on POSIX and fixes it for every consumer rather than just the test. That is the better fix if anything other than this test ever polls the feedback file — `/discovery` and `/draft-plan` both long-poll `/api/decision` rather than reading the file, so today the test is the only reader.

Verification: Both fixes shipped, because they close the race at different layers and the entry's own follow-up question turned out to matter. **Server side** — `annotate-server.js` gained `writeFileAtomic` (sibling temp + `renameSync`, falling back to a direct write if rename throws), and *both* `feedbackFile` writes now use it: steer mode and manual mode. The single-file mode check the entry asked for came back yes — `tests/annotate-server.test.mjs` had the identical wait-on-existence pattern at the manual-mode site, so it carried the same latent bug and was fixed too. The alternative the entry weighed was taken rather than the test-only fix: today the test is the only reader, but atomic-rename means a future reader cannot reintroduce this. **Test side** — a shared `readWhenReady(file, marker)` helper polls for *content*, and its `try/catch` turns an expired deadline into a failed match instead of an ENOENT stack trace.

The race was reproduced before it was fixed, which corrected the method on the way: a first harness "reproduced" nothing because the widened open→write window was a busy-wait in the same process, so the reader never got the event loop — single-threaded JS made the simulation vacuous. Re-run with the writer as a genuine child process (as the server is), the matrix is unambiguous: old writer + old reader reads `""` — the exact `Input: ''` symptom filed here — while old+new, new+old and new+new all read the complete file. Full suite 327/327 green. Released as condux 2.20.3, with a changeset for `@jabworks/condux` since `annotate-server.js` is bundled into `packages/condux-opencode/skills/plan-review/references/`.
